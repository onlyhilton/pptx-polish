---
name: pptx-polish
description: 把已有但有问题的 PowerPoint 演示文稿改造到可对外使用——四步流水线 + 一步收口：规范化（套 Reyee 模板版式、标题归位到占位符、字体归到主题）→ 配图（按型号从产品图库换高清图、认出 PPT 里的产品型号、修正图片被拉伸的显示框、删掉全透明的残留空图、规范图片排版）→ 排版（留白区不可侵犯、按内容选排列范式）→ 配色（单页不超过三色、优先品牌色）→ 标题单行收口（标题绝不换行，超长标题缩字号保持一行，用 PowerPoint 排版引擎核行数）。当用户上传或指定一份 .pptx 并希望"优化 / 规范化 / 套模板 / 整理配图 / 换高清图 / 图片糊了 / 图片被拉变形 / 删掉看不见的空图 / 排版 / 调色 / 标题换行了 / 标题两行 / 标题太长压到内容"时使用。在原件上原位改造，不重新生成。
agent_created: true
---

# pptx-polish · 已有 PPT 四步优化（+ 一步收口）

## 用途

把团队里收到的粗糙 PPT 改造到**可对外使用**的状态。四步依次递进，每步解决一类问题；最后一步是**收口**，必须在全部内容改动完成之后跑：

| 步骤 | 解决什么 | 交付标准 |
|---|---|---|
| 1 规范化 | 标题乱拉文本框、字体乱用 → 换模板必错位 | 不好看，但**规整** |
| 2 配图 | 纯文字或乱七八糟的配图；图片糊、被拉伸、残留空图 | 图文匹配、图片整齐、比例正确 |
| 3 排版 | 元素乱放、留白被侵占 | 留白区干净、排列有范式 |
| 4 配色 | 颜色多且杂、不符合 VI | 单页 ≤ 3 色、品牌色优先 |
| 5 标题单行收口 | 标题过长折成两行，第二行穿过标题下分隔线、把内容顶下去 | **每个标题都是一行**（放不下就缩字号） |

**在原件上原位改造，不重新生成。** 不使用 `slidep` 重建页面的路线——那会丢掉原件结构，与"规范化"的目标相反。

## 分步交付（默认）

每步产出独立文件，用户确认后进入下一步：

```
原名_1规范化.pptx → 原名_2配图.pptx → 原名_3排版.pptx → 原名_4配色.pptx → 原名_5标题单行.pptx
```

- 用户说"直接跑完 / 一键全跑"时，一次执行五步，只交付最终版。
- **Step 5 是收口步骤**：它必须在「内容与版面不再变动」之后运行 —— 任何改文字、改字号、改框宽的动作都会让它的结论失效，改完要重跑。
- 每一步的产物必须能独立打开、独立可用，不是中间半成品。
- 每步结束时给**该步的变更摘要**（改了什么、为什么、哪些没改），不写长篇报告。

## 红线

| 编号 | 红线 | 说明 |
|---|---|---|
| **R1** | 页序与页数不变 | 各步都不得增删页、不得调换页序（含 Step 5 收口）。确需增删时先停下来问用户 |
| **R2** | 内容事实零改写 | 数据、型号、参数、日期、客户名逐字沿用。禁止润色数字 |
| **R3** | 信息不删 | 可合并同类项、精简冗词，不得整条丢弃信息点 |
| **R4** | **真实图优先** | 产品图、截图、数据图必须用真实素材（官网 / 原件已有）。生图**只**用于概念图、氛围图，**禁止**用生图替代真实数据图或产品截图 |
| **R5** | 原件只读 | 输出新文件，对原文件不做任何写入 |
| **R6** | 留白区不可侵犯 | 母版安全区（默认左右 60px / 上下 20px，以模板为准）不得被内容侵占 |
| **R7** | **模板本体只读** | `ppt/slideMaster*` / `ppt/slideLayout*` / `ppt/theme/*` 是**模板格式的唯一事实源**，任何步骤都不得改写。不改版式的形状、几何、占位符槽位（含 `p:ph` 的 `sz`）、`defRPr` 的 `sz/b/i/u/spc/kern` 与 `latin/ea/cs`、颜色、背景图；不删版式、不删母版、不换主题。**只能"用模板格式改 PPT"（改页面），不能"用 PPT 的现状改模板"。** 确切需要动模板时（例如模板自己错了），先停下来说明改哪个版式、改什么、为什么，等用户明确同意。**交付前必须跑 `verify_template_fidelity.py` 且 P0 = 0** |

> R7 不是保险条款，是 2026-09-18 的实测事故结论：产物里 `Section Title` 版式的 `01` 占位符从
> `<a:defRPr sz="13800" i="1">`（斜体）被改成 `sz="13800" b="0" i="0"`（斜体没了），
> 同版式另一行 `b="1"` → `b="0"`、字体 `Noto Sans` → `Noto Sans Black`、
> `<p:ph sz="quarter">` 的 `sz` 被删、主题从 `Reyee` 换成 `ChatGPT`、母版被清空、16 个版式只剩 8 个。
> 而 `verify_pptx.py` **全过、P0 = 0** —— 它拿"已经改过的版式"当权威值，这一维根本不查。
> 用户的眼睛比体检表准：他第一眼就看出了"斜体被取消了"。

---

## 模板资产

**模板随包内嵌**在 `assets/master/`，命令里不写路径 —— `--theme` 默认取浅色模板。

| 别名 | 文件 | 底色 | 规模 |
|---|---|---|---|
| `default` / `light` | `2025 Ruijie Reyee PPT Template-20250530.thmx` | 浅色（默认） | 1.47 MB / 16 版式 / 7 嵌入字体 |
| `dark` | `2025 Ruijie Reyee PPT Template-Dark-20250620.thmx` | 深色 | 3.93 MB / 16 版式 / 9 嵌入字体 |

`--theme` 的解析顺序（`inject_template.py` 内 `resolve_theme()`，全程在包内、从 `__file__` 定位）：
**真实路径 → 包内裸文件名 → 别名 → 唯一子串 → 报错并列出可用清单**。
所以换机器、换 cwd 都跑得起来 —— 文件"在包里"不等于"能用包里的"，别在命令里写死绝对路径。
`.thmx` 是标准 OOXML 包，用 `zipfile` 直接读写。

### 模板的 16 个版式（**不是 6 个**）

用户口述的"六种版式"是**功能归类**；包内实际有 16 个 layout，其中封面按场景拆成 9 个。

| idx | 浅色版名称 | 深色版名称 | 归类 |
|---|---|---|---|
| 1 | General cover | General cover | **封面**（默认选它） |
| 2 | Office cover | Office cover | 封面 |
| 3 | Hotel cover | CCTV cover | 封面 |
| 4 | Retail cover | Retail cover | 封面 |
| 5 | CCTV cover | Hotel Cover | 封面 |
| 6 | Hostel cover | Hostel cover | 封面 |
| 7 | Home cover | home wifi cover | 封面 |
| 8 | Smar home cover | smart home | 封面 |
| 9 | Starlink cover | Starlink | 封面 |
| 10 | 目录 | Index1 | **目录** |
| 11 | Workshop index | Index | 目录（变体） |
| 12 | 标题和文本 | Title and content | **标题+内容** |
| 13 | 仅标题 | Title only | **仅标题** |
| 14 | Blank page | blank | **空白页** |
| 15 | Section Title | Section Title | 章节过渡 |
| 16 | 封底 | 封底 | **封底** |

> **两个模板的同名版式未必同 idx，且命名习惯不同**（浅色版叫 "General cover"，深色版同类叫 "smart home"；深色版内容页叫 "Title and content"）。
> 因此**严禁按 idx 或名字硬编码映射**，一律靠 `inject_template.py` 读占位符结构现场判定。

### 已实测的模板机制（改动前必须知道）

1. **版式背景是图片填充**（`<p:bg><p:bgPr><a:blipFill>` → `ppt/media/` 下的整页底图），**不是纯色**。
   → 深色版的深底全靠背景图。**注入必须把 media 一起搬**，只换主题色是无效的。这也是"换模板黑底变白底"的技术根因。
2. **母版本身没有任何占位符**，占位符全部由各 layout 自己定义。
3. **主题配色是 Office 默认**（`clrScheme name="Office"`，accent1=`5B9BD5`），**不是品牌色**；品牌蓝 `0055CD` 是**硬编码在 layout 的装饰形状**里的。
   → 正文若引用 `accent1` 会渲染成 Office 蓝，不是 Reyee 蓝。Step 4 要单独处理。
4. 字体方案 `fontScheme name="Reyee"`：latin = `Noto Sans`，ea = `微软雅黑`。两个模板都带**嵌入字体**（浅 7 / 深 9 个 `.fntdata`）。
5. **16 个版式全都没有 `sldNum`（页码）槽位** —— 模板自身不显示页码。
   → 源稿若带页码占位符（原稿版式有槽位、渲染正常），换模板后这些占位符**失接**：
   尺寸改由 master 继承（字号变大），框被压窄后 **"13" 会折成 "1"/"3" 两行**，
   掉下来的第二行正好压到左下 logo（`24,666,244,697`）上。**这是换模板最典型的「页码炸开」现象。**
   → 修法：**保持原稿的纵向位置**，只把框做宽 + 显式写死字号，令其单行。
     *不要*把它往上挪 —— 原稿页码本就紧贴在正文脚注下方（实测原稿 "13" 墨迹 y≈647.5–655，
     上方脚注墨迹止于 644），上移 6px 就会落进脚注那行（实测重叠 45%→100%，越修越糟）。
     *也不要*挪到右下——实测 P12/P16/P29/P31 等页右下有原稿内容。
     实测安全框：`off (24.8, 646) px` + `ext (60, 18) px` + `sz=1100` + 灰 `7F7F7F`：
     距上方脚注 2px、距 logo 顶端（666.4）2.4px，全部 17 页渲染通过。
6. **版式里那几件"看着多余"的东西其实都是设计语言，一个都别删**（2026-09-17 教训）：
   | 形状 | 几何（px） | 是什么 | 删掉的后果 |
   |---|---|---|---|
   | `矩形 7`（`仅标题` 里还多一个完全重叠的 `矩形 2`） | `L46 T55 R56 B99`，fill `00DB93`，`rot` 90° | **标题左侧那条绿色竖条** | 用户一眼看出"没用模板" |
   | `Image 21`（`<p:pic>`，引 `image18.png` + SVG 备用） | `64,113,998,116` | 标题下的**细分隔线**（落点常正好是原稿副标题的下划线，读起来是设计） | 标题区失去分割 |
   | `矩形 2`（封底里） | `92,293,571,427` | 封底的 **`THANKS!`**（76.2pt / `0055CD` / 非占位符） | 封底变成空白页 |
   这些形状都是 `p:nvPr/` **空**（非占位符），所以"占位符清单"里看不到它们，只有按形状点名枚举才会发现。
7. **换模板后 PowerPoint 会再建一套"母版+版式"**：`slideMasters/` 下出现 2 个 master、`slideLayouts/` 下出现 32 个 layout。**页面迁到新的那套（形状完整），旧的那套仍挂在 `presentation.xml` 的 `sldMasterIdLst` 里、并且在版式面板中排在最前**。审版式必须分母版（见陷阱表十七），交付前必须清孤儿母版（`scripts/dedupe_masters.py`）。
8. **标题的字体/字号是"版式槽位"决定的，不是页面**（2026-09-17 补，见陷阱十九）。模板 `.thmx` 实测值：

   | 版式 | 槽位 | 字号 | 字体 | 色 | 其它 |
   |---|---|---|---|---|---|
   | `标题和文本`（slideLayout12） | `type="title"` | **28.0pt**（`sz="2800"`） | `Noto Sans bold` | `043CC1` | `anchor="ctr"`，**无 `b` 属性** |
   | `General cover`（slideLayout1） | `type="ctrTitle"` | **44.0pt** | `Noto Sans`（**不带 bold**） | `0055CD` | `b="1"` |
   | 母版 `txStyles/titleStyle` | — | 26.0pt | `Arial` / `微软雅黑` | `043CC1` | **被 layout 覆盖，不是生效值** |

   → **改标题字号 = 改模板**（R7 禁止）。反过来，**要让某页标题回归模板，必须删掉页面上的覆盖，而不是在版式上再写一遍** —— 后者是把"页面不合格"改成"模板不合格"，方向反了。真要动版式槽位，先停下来征得用户同意。
   → 内容页标题色 `043CC1` 与品牌主色 `0055CD` **不是同一个值**，别混用（`0055CD` 是封面色 + 装饰形状色）。

#### 版式自带的装饰（= Step 3 的「保留区」，几何实测值）

画布 1280×720 px。这些形状**不属于任何占位符**，是 layout 里的独立装饰，内容不得压上去：

| 装饰 | 几何（L T R B，px） | 面积判定 |
|---|---|---|
| 右上 `RUIJIE｜Reyee` logo | `1013, 60, 1263, 96` | ≥ 3000 px² → **P1（LOGO）** |
| 左下 `Redefine your easy network` logo | `24, 666, 244, 697` | 同上 |
| 标题下分割线 | `64, 113, 998, 116` | < 3000 px² → P2（细装饰条） |
| 左上短横 | `29, 72, 73, 82` | 同上 |

- **内容页栅格左边界 = 64**（不是 44）；封面才是 44。内容页按 44 布就是越格。
- 保留区**动态读**（从 layout/master 的非占位符形状取），按几何去重（版式常有位置完全相同的重复装饰）。
- ⚠ **装饰形状的包围盒 ≠ 可见墨迹**：logo 素材自带约 30px 上内边距，脚注文字落在空白里会被算成"压 38%"。**位置交叠类判定必须渲染复核**。

---

## Step 0 · 结构体检与版式去重（可选前置）

长期迭代的 PPT 会在包里堆积**重复或从未被引用的 slideLayout**，后果不是体积（版式 XML 通常只占 0.x%），而是 PowerPoint"新建幻灯片"面板里出现大量同名/无用版式。

```bash
SKILL="${SKILL_DIR:-$HOME/.workbuddy/skills/pptx-polish}"
S="$SKILL/scripts"
PY="${PY:-python}"        # 需已装 python-pptx / lxml / Pillow；缺模块就把它指到你的解释器

# ① 版式指纹分析（只读）：结构分组、引用页数、未被引用清单
"$PY" "$S/dedupe_layouts.py" "<原件>" --out "<工作目录>/layout"

# ② 按 idx 删除指定版式（连同其 rels、master 的 sldLayoutId、Content_Types Override、专属媒体）
"$PY" "$S/dedupe_layouts.py" "<原件>" --only 17,18,19,20,21 --out-deck "<工作目录>/去重.pptx"

# ③ 体积分布 + 嵌入字体重复 + 孤立媒体
"$PY" "$S/size_diag.py" "<原件>"
"$PY" "$S/font_diag.py" "<原件>"
```

### 判据：三种"重复"性质完全不同，处置也不同

| 类型 | 识别特征 | 处置 |
|---|---|---|
| **历史导入副本** | 名字带 `1_` / `2_` 等序号前缀；或两个版式引用的背景图内容 hash 完全相同 | **可删**（零引用时直接删） |
| **模板自带的场景变体** | 占位符结构完全相同、仅背景底图不同（如 9 个行业场景封面） | **保留**——那是模板资产，交给用户决定 |
| **自建未引用版式** | 背景风格与模板不一致（如唯一的纯色底 vs 其余图片底），引用数 0 | 可删 |
| **降级副本（PowerPoint 重套模板的产物）** | **同名**（同 `cSld@name`）、占位符结构相同，但**少若干个装饰形状**；引用数 0 | 视同**整批**处置：连同它所属的**未被引用的孤儿母版**一起删 |

⚠ **`dedupe_layouts.py` 认不出"降级副本"**：它的指纹含形状清单，残缺版与完整版比就是"不同"→ 报"重复组 0"，只把它们列进"未被引用"。而 `--apply` 会**把未被引用但属于模板完整版式的那批也删掉**（实测 T3T4 那包会从 16 个版式只剩 6 个，Office cover / Hotel cover / Blank page / Workshop index 等全没了）——**此时不能用 `--apply`**，要走 `dedupe_masters.py`（按"母版是否被引用"整批处置，保留完整版式集）。

```bash
# ④ 孤儿母版清理（换模板后必做）：未被任何页引用的 master + 它的全部 layout + 专属 theme/tags
"$PY" "$S/dedupe_masters.py" "<原件>"                  # 只分析
"$PY" "$S/dedupe_masters.py" "<原件>" --out-deck "<工作目录>/清母版.pptx"
```

### 实测数据（BYOD Office Network v10，2026-09-16）

21 个版式中 14 个从未被引用（85.8 KB XML + 0.92 MB 专属媒体）。删 5 个历史副本后净减 0.78 MB，**30 页挂载关系零变化**。

**体积的真相必须核实后再向用户陈述**——同一份文件的实际分布：

| 部件 | 体积 | 占比 |
|---|---|---|
| `ppt/media` | 31.39 MB | 74.6% |
| `ppt/fonts` 嵌入字体 | 9.79 MB | 23.3% |
| `ppt/slides` | 0.65 MB | 1.6% |
| `ppt/slideLayouts` | **0.14 MB** | **0.3%** |

**用户凭直觉说"版式重复增加体积"时，要如实纠正方向**：真正的大头是媒体与嵌入字体。该样本的 9.79 MB 字体里有 **7.52 MB 是纯重复存储**（5 个文件内容完全相同、4 个完全相同）——但合并需要 Office 端验证，不可在无渲染环境的前提下擅自动手。

---

## Step 1 · 规范化（Normalize）

目标一句话：**让这份 PPT 虽然不好看，但很规整**。

### 1.0 先判断：这份 PPT 是否已经套过模板（必做）

**不看这一步就跑 `inject_template.py`，可能白干、甚至叠加第二套母版把文件弄脏。**

```bash
"$PY" - <<'PYEOF'
import zipfile, re
z = zipfile.ZipFile("<原件>")
for t in sorted(n for n in z.namelist() if re.match(r"ppt/theme/theme\d+\.xml$", n)):
    x = z.read(t).decode("utf-8", "ignore")
    print(t,
          "fontScheme=", re.search(r'<a:fontScheme name="([^"]*)"', x).group(1),
          "clrScheme=", re.search(r'<a:clrScheme name="([^"]*)"', x).group(1))
print("版式数", len([n for n in z.namelist()
                    if re.match(r"ppt/slideLayouts/slideLayout\d+\.xml$", n)]))
PYEOF
```

| 指纹 | 判定 | 动作 |
|---|---|---|
| `fontScheme` 已是目标模板名、版式名与模板一致 | **已套过** | **跳过 1.1**，只做 1.2 |
| `fontScheme` 是 Office / 默认值，版式是 Office 那套 | 未套 | 1.1 → 1.2 |
| 版式数 ≠ 模板版式数（例 21 ≠ 16） | 包内有自建/残留版式 | 记录并交人工判断是否清理 |

**实测教训**（2026-09-16）：`BYOD Office Network v10.pptx` 看着"很不规范"，但它**已经套了 Reyee 模板**（`fontScheme=Reyee`、版式名与模板一致、画布尺寸一致）。真正的问题只是"标题没归位 + 字体没继承"。客户给的不规范文件未必是"没套模板"，更常见的是"套了但没归位"。

### 工具链（顺序固定）

```bash
PY="${PY:-python}"        # 需已装 python-pptx / lxml / Pillow；缺模块就把它指到你的解释器
SKILL="${SKILL_DIR:-$HOME/.workbuddy/skills/pptx-polish}"
S="$SKILL/scripts"

# ① 看模板版式清单 + 目标文件的自动映射建议（不写文件）
"$PY" "$S/inject_template.py" --source "<原件>" --outline

# ② 套模板：注入母版体系 + 逐页重映射版式
"$PY" "$S/inject_template.py" --source "<原件>" \
      --out "<工作目录>/1注入.pptx" --sync-size
#    模板不用给路径（默认浅色底）；要深色底加 --theme dark
#    自动映射不对时用 --map "1:2,2:10,3:12,5:16" 覆盖

# ③ 标题归位 + 字体继承
"$PY" "$S/normalize_deck.py" "<工作目录>/1注入.pptx" --out "<工作目录>/1规范化.pptx"

# ④ 交付前体检（P0 必须为 0）
"$PY" "$S/verify_pptx.py" "<工作目录>/1规范化.pptx" --out "<工作目录>/verify"

# ⑤ 模板保真体检（R7；P0 必须为 0）—— 比对版式/母版与 .thmx 是否逐形状、逐 run 属性一致
"$PY" "$S/verify_template_fidelity.py" "<工作目录>/1规范化.pptx" --json "<工作目录>/fidelity.json"

# ⑥ 真机打开测试（**换模板之后不可省**）—— E_FAIL 只有这一步拦得住
"$PY" "$S/open_test.py" "<工作目录>/1规范化.pptx"
```

### 1.1 套用模板版式（`inject_template.py`）

在 OOXML 包层做"换模板"：

- 把模板的 `slideMaster` / `slideLayouts` / `theme` / `media` / `embeddings` / 嵌入字体整体注入目标包，所有部件加前缀（默认 `tpl`）避免命名冲突；
- 重建关系链（`.rels`）与 `[Content_Types].xml`；
- 逐页重映射到模板版式；
- **默认删除目标原有的旧母版与旧版式**（`--drop-old-master`）——否则 PowerPoint 的版式列表会同时出现两套。仅当所有页面都成功重映射时才执行删除，否则自动放弃并告警；
- **同时回收旧主题**（2026-09-18 补）：删旧母版时把 `presentation.xml.rels` 的**演示文稿级主题**改指到模板主题（`tplThemeN.xml`），并把"改指之后无人引用"的旧 theme 一起删掉。判定按**血统**（这个 theme 挂在被删母版下），不按引用计数 —— 旧 theme 被"旧母版 + presentation"双引用时计数=2，孤儿扫描不敢动它，于是包看着换过模板、主题库里仍留着旧模板的血统。`notesMaster` 自己的主题是**合法独立血统**，会被保留（不误删）；
- `--sync-size` 把画布同步为模板的 16:9。

映射判定依据是**占位符结构**，不是名字。`auto_map()` 的判定顺序（**必读**，顺序即优先级）：

| 序 | 判据（`| ` 前是条件） | 判定 |
|---|---|---|
| 0 | 第 1 页 / 末页 | 封面 / 封底 |
| 1 | 标题含 目录·agenda·contents / thank·封底·Q&A | 目录 / 封底 |
| 2 | 有 `ctrTitle` | 封面 |
| 2 | 无 `title`，body 成组（≥4 个且 idx≥17）**且每行 ≤40 字、合计 ≤200 字** | 目录 |
| 2 | `title` + body 成组，同样字样限制 | 目录 |
| 2 | 无 `title`，有 body，合计 ≤80 字 | **章节过渡** |
| 2 | 无任何占位符，**且页面当前挂的版式本身也没有占位符** | 空白页（满版设计） |
| 3 | 文字 ≤ 标题+8 且 < 60 字 | 仅标题 |
| 3 | 其余 | 标题+内容 |

> **2026-09-18 修掉的一个硬伤**：旧 `auto_map()` 只按"文字长度"猜角色，第 2 档整档不存在，
> 于是"无 title、两个 body 槽位(17/22)、合计 12 字"的章节过渡页被判成 `title_only` ——
> 注入后章节页拿不到 `Section Title` 版式的 `01` 序号位与蓝带，**整页退化成裸文字**。
> `KIND_SECTION` 一直是"定义了却从不产出"的常量。运行 `--outline` 时**逐页会打印判定依据**，
> 一定要扫一眼有没有"标题+内容"里混进真正该做章节过渡的页。
> 阈值常量（`BODY_GROUP_MIN` / `TOC_LINE_MAX` / `TOC_CHARS_MAX` / `SECTION_CHARS_MAX`）在
> `inject_template.py` 顶部，改它们要同步改这张表。

**孤儿占位符检查**：若原页面的占位符在新版式里找不到对应槽位，内容会孤悬。工具会逐页列出，**必须处理完再进下一步**。

### 1.2 标题归位（`normalize_deck.py`）

**凡是有标题的页面，标题必须写在 title 占位符里，不是随便拉一个文本框。**

这是"换模板时大面积错位"的根因：自由文本框不继承版式，换主题后位置与配色全部失效。

- 判定是**版式感知**的：封面类版式（有 `ctrTitle`）在全页找字号最大的短文本；内容类版式（有 `title`）只在上部区找；
- **版式没有标题槽位的页面（封底 / 空白页）跳过并报告**，不硬塞——那会破坏设计；
- 新建的占位符**不带坐标**（不写 `<a:xfrm>`），位置与尺寸完全继承版式，这样以后再换模板标题会自动跟着走；
- 同时清除所有 run 上硬编码的字体（`a:latin`/`a:ea`/`a:cs`），让文字继承主题字体。

### 1.3 交付判定

- `verify_pptx.py` 报 **P0 = 0**（无悬空引用、无元素顺序错误、Content_Types 完整）；
- `verify_template_fidelity.py` 报 **P0 = 0**（R7：版式/母版/主题与 `.thmx` 逐形状、逐 run 属性一致）；
- 有标题的页面，标题**全部**在 title/ctrTitle 占位符里；
- 全篇无硬编码字体残留；
- 此时换任何同类模板都不会错位。

> **这两份体检的分工不能混**：`verify_pptx.py` 查"包结构 + 页面有没有归位"，
> 它的权威值是**产物自己的版式** —— 版式被改了它也照样报 P0=0；
> `verify_template_fidelity.py` 查"版式本身有没有被改"，权威值是**模板文件**。
> 只跑前者，"模板被篡改"这条路径全程静默。

### 1.4 嵌入字体去重（`dedupe_fonts.py`，体积优化）

PowerPoint 嵌入字体时会**把同一个字体文件重复写进包里**。实测 BYOD 那份：`ppt/fonts/` 12 个部件 9.79 MB，其实只有 5 份不同的字节 —— `font5/font6/font8/font9/font12` 是同一份（1.15 MB × 5），`font3/font4/font7/font11` 是同一份（0.97 MB × 4）。**8 个部件纯冗余，占 7.52 MB。**

先看现状再决定动不动：

```bash
python font_diag.py deck.pptx              # 打印重复指纹（不改文件）
python dedupe_fonts.py deck.pptx --dry-run # 报告能省多少
python dedupe_fonts.py deck.pptx -o deduped.pptx
```

做法与安全边界：

- **只合并"字节完全相同"的部件**（连 OOXML 的 32 字节混淆 GUID 头都一致）。字节相同 ⇒ 解出的字体完全相同 ⇒ 只是不再重复存储。
- **绝不做字体子集化/裁剪** —— 不存在"少了某个字形"的风险。
- 改动只有两处：`ppt/_rels/presentation.xml.rels` 里被合并的 `rId` 的 `Target` 指向保留的那份；删除被合并的 `ppt/fonts/*.fntdata`。`presentation.xml` 的 `embeddedFontLst` **一个字都不用改**（`r:id` 引用关系没变，只是关系指向同一目标）。
- `fntdata` 在 `[Content_Types].xml` 里是 `Default Extension` 声明，**删部件无需改 Content_Types**。
- 实测：其他部件**字节零变化**；30 页渲染逐像素比对，**29 页差异为 0**，P23 仅 1269 像素有 `max=2` 的舍入差异（图片缩放的抗锯齿噪声，非字体）。

**交付前的验证是强制的**，不能只跑 `verify_pptx.py`：

```bash
python verify_pptx.py deduped.pptx                     # P0 必须为 0
python render_deck.py  deduped.pptx --out r-new        # 与去重前的渲染逐页比对
```

> 为什么必须渲染比对：`verify_pptx.py` 只保证**包结构合法**，保证不了"PowerPoint 认不认多个 `<p:bold>` 指向同一关系"。字体一变，文字排版就会移位，只有逐页图像比对能证明"没变"。

---

## Step 2 · 配图（Illustrate）

> **先渲染再动手。** 跑 `render_deck.py` 拿到逐页 PNG，对着图判断"这页的图配不配文字、糊不糊"。
> 只看 `native_px` / `zoom` 数字能判出"分辨率不够"，但判不出"这张图跟这页内容有没有关系"。

### 2.1 盘点现有配图

先跑 `audit_deck.py --extract-images` 把原件图片全部落盘（**不过滤小图**，小图标记"低分辨率，需确认是否重制"），再分四类逐张给处置：

| 分类 | 处置 |
|---|---|
| 可用 | 保留，参与统一规格 |
| 需替换 | 图不对文（风格不符、画质低、与内容无关）→ 换 |
| 需删除 | 纯装饰性小贴片、水印图、无关图 → 删 |
| 需新增 | 有文字描述但无图 → 补 |

### 2.2 配图来源

**真实图优先，生图补位**：

1. **产品图 / 设备图** → **产品图库（首选，按型号取，见下）**；官网、产线素材次之
2. 界面截图 → 原件已有素材、系统实机截图
3. 数据图 / 图表 → 依据原件数据用矢量图表重绘（数值逐字核对）
4. 概念图 / 氛围图 / 抽象示意 → 可用生图（Ruijie GPT-image-2）

生图产物必须落到项目 `assets/` 后再引用，并逐张核对内容与比例。

#### 产品图库（随包分发，已建索引）

**图库随 skill 包分发在 `assets/product_images/`**（311 个 webp / 37.6 MB / 17 个系列），
索引 `assets/product_index.json`：**310 条 / 309 个型号**。

根目录解析顺序（`product_lib.find_root()`）：
`--root` → 环境变量 `RUIJIE_PIC_ROOT` → **包内 `assets/product_images/`** → 本机 OneDrive 外部库 → 整个 `产品图片/`（多库模式兜底）。
**包内优先**，保证不同机器上取到的是同一套素材。

| 子库 | 条目 | 型号 | 体积 | 用不用 |
|---|---|---|---|---|
| **`assets/product_images/`（包内）** | **310** | **309** | **37.6 MB** | ✅ **默认素材源** |
| `Webp/`（本机外部库） | 310 | 309 | 37.6 MB | 与包内同源。只有 OneDrive 里新加了型号、要跟随最新时才 `--root` 指过去 |
| `PNG/` | 289 | 279 | 1175 MB | ❌ 默认不扫。只在要 PNG 独有的 8 个型号时 `--root` 切回全库 |
| `云桌面/` | 164 | 90 | 283 MB | ❌ 默认不扫。云桌面终端 CT/CPM/CPK 系列，**包内没有** |
| `Cybrey/` | 365 | 305 | 56.5 MB | ❌ 文件名是"正/右/顶/俯视""105.307"方位词，不是型号，别拿来匹配 |

**跨机器怎么还能找到图（本次改造的核心）**：

索引每条记录同时存两个路径字段——`rel`（相对图库根，如 `RG-ES/RG-ES106D-P V2.webp`）与 `path`（建库时的绝对路径）。
`load()` 返回前一律过 `rebase()`：只要索引带 `root_rel`（图库在包内就会带），就用 `<SKILL_DIR>/<root_rel>/<rel>` 把 `path` 重算成本机真实位置。
→ 包解压到任何路径都能直接取图，**消费方（`replace_image` / `match_product`）一行都不用改**。

判断"要不要重建索引"用的是 `root_key`（包内 → `skill:assets/product_images`；外部 → `abs:<绝对路径>`），**不是绝对路径**——否则同一份包换个机器就被误判成"图库挪位"而重建。

> **同步外部库的新图**：包内是**静态副本**，OneDrive 里新增的型号不会自动进包。要更新走两步——
> ```bash
> # ① 把外部库覆盖进包内（或只补新增文件）
> # ② 重建索引（会自动认到包内目录）
> python product_lib.py --build
> ```
> 只在本机临时用外部最新库、不想动包：`--root "<你的图库根>/Webp"`（或设 `RUIJIE_PIC_ROOT`）。
> 未指 `--root` 时，外部库按 `OneDrive` / `OneDriveConsumer` / `OneDriveCommercial`
> 环境变量推导到 `<OneDrive>/PIC/产品图片`（子路径见 `product_lib.PIC_SUBPATH`），**不写死盘符**。

**为什么 Webp 就该是唯一默认源（实测，不是偏好）**：

1. **覆盖最全。** Webp 309 个型号 > PNG 279 个 —— Webp 比 PNG 多 30 个型号（`RG-CAP` 全系、`RG-ANT30/90`、部分 e-Lighten SFP）。PNG 不是超集。
2. **体积 1/35。** 两库共有的 271 个型号里 **268 个像素尺寸完全相同**：webp 侧合计 32.7 MB，png 侧 1147.5 MB。画质实测平均色差 1.4–4.0 / 255、P99 ≤ 16、**alpha 通道完全一致**——PPT 显示最大也就 500 px 上下，看不出来。
3. **95% 真透明**（alpha 覆盖率 > 2%），可直接压深色底。

**包内图库的覆盖缺口（写清楚，别到用的时候才发现）**：PNG 独有 **8 个型号**（锐捷自有 3 个：`RG-EST300F-P`、`RG-NBR6125-E`、`OM-GE-10KM-SFP-SM1490`；竞品 5 个：`SG5200`/`TP-EAP225`/`U6 LITE`/`UAP-AC-M`/`UDM PRO`）+ 云桌面终端整条线 **90 个型号** —— 这些**都不在包里**。对外分发后若命中这些型号，只能继续从厂商官网取图，或自行补进 `assets/product_images/`；本机可 `--root "D:/OneDrive/PIC/产品图片"` 切多库模式补取，代码不用改。

命名规律与归一化（附属标记剥离后同型号归一条 key）：

```
<型号>.webp                   主图
<型号>-Front / -Side          视角变体   tags: view:front / view:side
<型号>-1                      同型号第 2 张   tags: seq:1
<型号>-Front-175mm            带尺寸工程图   tags: view:front, dim:175mm
<型号> for US / V2 for Canada 区域版   tags: region:US
<名称>-removebg-preview       抠图残留后缀   tags: removebg
已停产/<型号>.webp             停产目录 → active:false，排序永远排最后
```

> ⚠ **序号剥离只允许匹配单个数字**（`-\d$`）。写成 `-\d{1,2}$` 会把 `RG-CS88-08`、`RG-ANT20S-90`、`RG-NIS-PA120-48/54` 这些**真实型号尾数**腰斩，705 个型号被错误合并。改完要审计"所有 key ≠ 文件名主干"的条目，确认每一处剥离都是视角/序号/尺寸/区域标记。

> ⚠ **图库里有 `已停产/` 目录**（`RG-ES` 17 个、`RG-RAP` 2 个），型号名与在售目录**完全相同**。撞键时**在售的赢**（实测 `RG-ES106D-P V2` 取到 `RG-ES/ES1/` 那版）；只有停产型号才落 `active:false`，排序永远排在在售之后。

> ⚠ **`load()` 必须无条件拿 `find_root()` 与索引里的根目录比对**（现在比的是 `root_key`）。原先只在显式传 `--root` 时才比对，导致把图库从 `产品图片/` 改成 `产品图片/Webp/` 之后旧索引被静默沿用，`--new-model` 取到的还是旧库的图。
>
> ⚠ **`root_key` 绝不能用绝对路径**。图库随包分发，建库时的绝对路径在别人机器上必然对不上，会被误判成"图库挪位"而触发重建（要求对方装了 numpy/Pillow，还要跑几十秒）。包内目录一律记 `skill:<相对 SKILL_DIR 的 posix 路径>`。

### 2.3 认型号：PPT 上那张图是哪个型号（`match_product.py`）

图库解决"知道型号就能取图"，但还差一半：**PPT 上那张小图是什么型号？** 这一步把闭环补上：

```bash
python match_product.py deck.pptx --out 型号匹配.md
python match_product.py deck.pptx --pages 22,23 --min-score 0.93
```

原理：内容裁切 → 白底合成 → 保比例归一化 → 与图库特征矩阵做相关度匹配，再用**长宽比门控**（容差 1.35）过滤。图库特征有 `.npz` 缓存，第二次跑是秒级。

| 条件 | 结论 |
|---|---|
| 匹配分 ≥ 0.93 **且**长宽比通过 | 高置信，**可直接换** |
| 0.72 ≤ 分 < 0.93 **且**长宽比通过 | 疑似，报告里列候选 top3 供人工挑 |
| 其余 | 认不出（非产品图 / 库里没有） |

**校准过程（照抄结论，别重走弯路）**：

| 试过 | 结果 |
|---|---|
| 阈值 0.72、无门控 | ❌ 机柜照、办公室照都被"认出"成天线/交换机，**假阳性成灾** |
| 加**长宽比**门控 | ✅ 有效。真匹配的图形比例几乎一致（`image77` 4.28 vs 4.31） |
| 再加**留白占比**（`fill`）门控 | ❌ **失败**。图库 PNG 是紧贴裁切的产品图，`fill` 会把产品本体算成内容，连 0.999 的真匹配都被误杀。**留白不是可靠判据** |
| 阈值定 0.93 | ✅ 0.88–0.93 区间真假混杂（`image85` 0.917 是真、`image26` 0.893 是假），所以这一区间一律标"疑似" |

**已人工复核的正确样本**（放大逐对比对过）：`image76→RG-NBS5200-24SFP8GT4XS` 0.989、`image85→RG-NBS3100-24GT4SFP-P` 0.917、`image81→RG-RAP1260` 0.969、`image77→RG-EG210G-P` 0.999、`image89→RG-RAP72` 0.991（已实际换图验证）。

**必须让用户知道的一件事**：**案例实拍照片不在产品图库里。** BYOD 那份 PPT 里被标"糊"的 4 张（P28 办公室、P28 机柜、P29 前台壁画、P29 机柜）全是现场照片，`match_product.py` 报"认不出"是**正确行为**，不是工具失灵。这类只能重拍或换现场照，跟型号无关。

### 2.4 换高清图（改图，不改版面）— `replace_image.py`

**"图糊了"的正确修法是换位图，不是改版面。**

页面上图片的位置和尺寸由 `<a:xfrm>` 决定，与图片本身多少像素**完全无关**。所以：

```
图片框（off / ext / srcRect）一个字节不动   →  渲染尺寸与位置必然不变
只换 ppt/media 里的位图数据                →  清晰度提升
```

任何"把图片框改大 / 挪位置 / 改比例"的修法都是错的——那是在动版面，不是在修画质。

```bash
# ① 按型号取图（走图库索引，不用写路径）—— 首选
python replace_image.py deck.pptx --slide 23 --pic 10 \
    --new-model RG-RAP72 --match body --out fixed.pptx

# ② 批量按型号：媒体部件 = 型号
python replace_image.py deck.pptx \
    --map-model "image89.png=RG-RAP72,image85.png=RG-NBS3100-24GT4SFP-P" \
    --match body --out fixed.pptx

# ③ 直接给路径（图库没有的素材）
python replace_image.py deck.pptx --slide 23 --pic 10 \
    --new "hi.webp" --match body --out fixed.pptx

--match content   按"内容足迹"（含阴影/光晕）等大对位 —— 通用默认
--match body      按"产品本体"（高不透明度实体区）等径对位 —— **产品图用这个**
--scale 0.93      在对位基础上再微调内容占比
--canvas-scale 16 画布相对旧图的**放大上限**（实际倍率自适应到"素材原生 : 旧图参照框"≈1:1 就停）
--lib webp|png    强制用哪个库（默认不指定 —— 索引单库时全库就一套）
--variant front   指定视角
--min-oversample  素材相对显示尺寸最低过采样倍率（默认 1.5）
--dry-run         只出预览 PNG，不写包
```

> ⚠ **`--canvas-scale` 是上限不是定值。** 写成固定 ×16 会出事：旧图 78×62 时正好（素材 1238 px ≈ 78×16），但**同一张图换过第二次**时旧图已变成 1248×992，再 ×16 就是 3.17 亿像素画布 → PIL 抛 `DecompressionBombError`；就算不崩，素材也会被上采样十几倍反而变糊。现在倍率自适应（素材原生 : 旧图参照框 ≈ 1:1 就停），再叠 4000 万像素硬上限。

> **图库素材一律 `--match body`。** 实测这套素材是**紧贴阴影裁切**的，用 `--match content` 会让产品本体大 7%。

**两个必须知道的对位细节**：

| 坑 | 说明 |
|---|---|
| **紧贴裁切的产品图会大一圈** | 素材若是**贴着阴影裁切**的（圆盘几乎占满画布），直接按内容足迹对位，产品本体会比原来大 7% 左右。实测 BYOD P23：内容对位 → 本体 95.5px（旧 89.3）；改用 `--match body` → 89.4px，**机身等径** |
| **重采样会渗灰边** | 透明素材缩放时若 RGB / alpha 分别重采样，透明区的脏 RGB 会渗成灰边。脚本用**预乘 alpha → 缩放 → 反预乘**处理 |

脚本会打印几何对照（旧/新画布、内容、本体、渲染尺寸），并校验素材有没有被推出画布边界。
**换完必须重跑 `verify_pptx.py`（P0 = 0）+ `render_deck.py` 复核**——只看数字不算验证。

### 2.5 图片排版形式

`横排` / `纵排` / `田字格`（2×2）/ `九宫格`（3×3）—— 按图片数量与内容关系选：

| 图片数 | 推荐形式 |
|---|---|
| 1 | 单图主视觉（占内容区 ≥ 40%）或半屏 |
| 2 | 横排并列 或 左右分栏 |
| 3 | 横排三等分 |
| 4 | 田字格 2×2 |
| 6 | 3×2 网格 |
| 9 | 九宫格 3×3 |

### 2.6 规格统一（"整齐"的判定）

- 同一组图片**统一高度**或**统一宽高比**，不得混用；
- 等间距（水平/垂直间距一致）、边缘对齐；
- 不得拉伸变形（宽高比与原始比例偏差 > 5% 即不合格）；
- 统一圆角与描边风格。

### 2.7 修几何：消变形与删残留（`fix_geometry.py`）

审计报的图片问题里，有两类是**零素材依赖**的，不用找新图就能修掉——先看能不能走这两条，再考虑换素材。

```bash
# 消变形：把显示框宽高比校正到与「有效原图区域」一致
python fix_geometry.py deck.pptx --slide 22 --shape-id 58 --fix-aspect -o fixed.pptx
python fix_geometry.py deck.pptx --slide 22 --shape-id 58 --fix-aspect --keep width --dry-run

# 删残留：删形状 + 该形状独占的关系 + 无人引用的媒体部件（父组合变空则一并删）
python fix_geometry.py deck.pptx --slide 25 --shape-id 96 --drop -o fixed.pptx
```

定位用 `--slide N --shape-id ID`，**与 `audit_deck.py` 报告的 `p{slide}_s{id}` 一一对应**（报告里的 `p22_s58` = 第 22 页 shape_id 58）。

| 动作 | 适用 | 关键点 |
|---|---|---|
| `--fix-aspect` | 有效区域比例 ≠ 显示框比例（审计的 `distort_pct > 5`） | 左上角不动；默认 `--keep height` **只改宽**，这样下方有文字也不会压到。修完 `distort_pct` 应降到 < 1% |
| `--drop` | `blank: true` 的空图、确认过的编辑残留 | 级联清理，不留孤儿；**空图删除后渲染应当逐像素零差异**——若有差异说明它不是空图 |

**什么时候不该消变形**：先看这个"变形"是不是设计意图。审计已经扣除了 `srcRect` 裁切，剩下的才是真变形。但即使是真变形，也先看**改动代价**：`--keep height` 会让图变窄（产品视觉变小）、`--keep width` 会让图变高（可能压下方元素）。先跑 `--dry-run` 看两个数再决定，必要时对照渲染图问用户选哪个。

**改完必须双验证**：

1. `verify_pptx.py` → P0 = 0；
2. `render_deck.py` 渲染相关页 + 逐像素比对 —— **差异必须严格落在被改元素的那个矩形内**。如果别的区域也有差异，说明字符串替换误伤了。

## Step 3 · 排版（Layout）

> **先渲染再动手。** "留白区是否被侵占"用包围盒判会大量误报（见「审计判定的六个陷阱」之「声明包围盒 ≠ 可见墨迹」），
> 必须对着渲染图看。改完排版也要再渲染一次复核，视觉节奏的变化只能看图确认。

### 3.1 留白区不可侵犯

母版安全区（默认左右 60px / 上下 20px，以模板为准）内不得放内容。页脚区、页码位、模板装饰位不得被正文挤占。

### 3.2 排列范式

按**内容之间的关系**选范式，不按好看选：

| 内容关系 | 范式 | 说明 |
|---|---|---|
| 并列、无先后 | 横排 / 田字格 / 九宫格 | 等权重的条目 |
| 递进、有阶段 | 阶梯 | 逐级上升的路径 |
| 层级、有包含 | 金字塔 | 从上到下的包含关系 |
| 时序、有先后 | 纵排 | 时间线、流程 |
| 对比、两端 | 左右分栏 | 前后 / 优劣对比 |

### 3.3 整体检查

- 每页内容占比在 30%–85%，无溢出；
- 相邻页版式不重复，避免"流水线感"；
- 同类页面的标题位置、页码位置一致。

## Step 4 · 配色（Color）

### 4.1 颜色数量

**单个页面颜色不超过三个（黑、白除外）。**

- 计入数量的是有彩色的元素：文字色、形状填充、线条、图表系列色；
- 黑、白、以及中性灰不计入；
- 全篇的颜色总数也应尽量少——同一角色用同一个色。

### 4.2 品牌色优先

| 名称 | 规范值（用户提供，2026-09-16 确认） | HEX |
|---|---|---|
| Reyee 蓝 | R0 G85 B205 | `0055CD` |
| Reyee 绿 | R0 G220 B165 | `00DCA5` |

> **口径已确认**：用户明确"模板制作时品牌色没有精确录入"，所以**以规范值为准**，把原稿的近似色统一过来。
> 模板包里 `0055CD` 一致，但 `00DCA5` 出现 0 次（模板实际用的绿是 `1CDB93`/`00DB93`/`00DBA3` 等近似值）—— 这是制作误差，不是新规范。

### 4.3 颜色替换必须按角色分治（**核心红线**）

**同一个色值可能同时用于"填充"和"文字"，两者不能用同一个目标色去替换。**

实测：`#008E7B`（深绿，32 处**文字**）如果统一成品牌绿 `#00DCA5`，白底对比度会从 **4.07:1 掉到 1.78:1**（WCAG 要求 4.5:1）—— 文字直接不可读。原稿的分工是刻意的：

| 用途 | 应换到 | 理由 |
|---|---|---|
| 填充 / 线条 / 渐变 / 阴影 | **品牌色原值**（`0055CD` / `00DCA5`） | 装饰用亮色没问题 |
| 文字 | **保持深色层次** | 亮色文字在浅底上读不了 |

`recolor_deck.py` 的 `--only-role` 就是为这件事准备的：

```bash
"$PY" "$S/recolor_deck.py" "<in.pptx>" --report --out-dir "<dir>"        # ① 先普查（含角色分布）
"$PY" "$S/recolor_deck.py" "<in.pptx>" --map "0B49C1=0055CD,EEF4FF=F0F6FF" --out "<out.pptx>"
"$PY" "$S/recolor_deck.py" "<in.pptx>" --map "00BF8F=00DCA5" \
      --only-role "fill,line,gradient,shadow" --out "<out.pptx>"          # ② 绿系只动装饰
"$PY" "$S/recolor_deck.py" "<in.pptx>" --map "00BF8F=008E7B" \
      --only-role "text" --out "<out.pptx>"                               # ③ 绿文字统一到深色版
```

> **替换范围默认只含页面**（`ppt/slides/` + `ppt/notesSlides/`）。版式与母版是**模板本体**，
> 默认不改 —— 改它们就不叫"套模板"，叫"改模板"（R7）。
> 报告里若出现"模板本体里也有这些色值（默认不改）"，那是**提示你色值分歧的根源在模板**，
> 正确处置是把这个分歧原样报给用户，而不是顺手把模板改了。确实要改才加 `--include-template`。
> `fix_title_style.py --unify-title-color` 同理：默认只改页面，版式/母版要 `--allow-template-write`。

### 4.4 实测的映射配方（可直接复用）

| 动作 | 映射 | 覆盖角色 |
|---|---|---|
| 蓝系统一 | `0B49C1` `043CC1` `143DC1` `1462C8` `3380FF` `0055CC` `123D78` → `0055CD`；紫色 `7758D9` → `0055CD` | 全部 |
| 浅蓝底统一 | `EEF4FF` `DCE6EE` `DDE8F0` `D8E2EA` `D4E0EA` `C9D8E6` `C9DDF3` `F2EEFF` → `F0F6FF` | 全部 |
| 绿系装饰统一 | `00BF8F` `00DB93` `1CDB93` `00EDB4` `00DBA3` `90E2D3` `00DBFF` `00B98C` → `00DCA5`；纯黄 `FFFF00` → `00DCA5` | 仅 fill/line/gradient/shadow |
| 浅绿底统一 | `EAF9F4` → `F0FFF4` | 仅 fill/line/gradient/shadow |
| 绿文字统一 | 6 个绿值 → `008E7B`（原稿事实标准，不引入新色） | 仅 text |

效果：有彩色 **41 → 17 种**，单页超 3 色 **16 页 → 0 页**。

**浅色底怎么推**：品牌色保持 HSL 色相与饱和度、明度提到 97%。`0055CD`(H215 S100 L40) → `F0F6FF`；`00DCA5`(H165 S100 L43) → `F0FFF4`。

深色版的强调色逻辑与浅色版不同：浅色版主色是品牌蓝 `0055CD`；深色版**完全没有蓝**，改用 `FBAE40`（琥珀黄）+ 绿。

- 已应用模板主题时，可考虑改用主题色槽位（accent 色）保证换主题能跟随 —— 但本模板 `accent1` 是 Office 默认 `5B9BD5`，**不是品牌蓝**，直接引用会跑色。

---

## Step 5 · 标题单行收口（Title One-Line）· **最后一步**

> **口径（2026-09-18 用户确认）**：**标题绝不换行。** 放不下就缩字号。
> 这条优先级高于"标题字号严格等于模板值" —— 模板标题框只有**一行高**，折行会穿过标题下的分隔线并把正文顶下去，
> 那是把模板设计破掉，比字号小 2pt 严重得多。

### 5.1 为什么必须由"排版引擎"来判，不能用 XML 量

标题框高 `50.5pt`、字号 `28pt`、容行数 **1**。折行与否取决于**字体度量 + PowerPoint 的断行算法**，
XML 里只有"框宽 / 字号 / 内边距"，读不出断行位置。三种判法的可靠性完全不同：

| 判法 | 可靠性 | 说明 |
|---|---|---|
| 用 PIL 量字宽 vs 框宽 | **只能预测** | 与 PowerPoint 实际断行有亚像素差，边缘案例会猜错。适合**算缩放比**，不适合下结论 |
| 渲染 PNG 数墨迹行 | **不可靠** | 装饰线/图标会干扰；而且**第二行溢出到框外，裁切时就被切掉了** |
| `TextRange.Lines().Count`（PowerPoint COM） | **事实源** | PowerPoint 自己的行数。`verify_title_lines.py` 走这条 |

> 连 `TextRange.Font.Size` 都读得到**生效字号**（含 `fontScale` 折算后的值）—— 这是"缩放真的生效了"的直接证据。

### 5.2 缩字号怎么写：`fontScale` 而不是改 `sz`

**默认 `--mode autofit`**：把 `fontScale` 写到**页面**占位符的 `bodyPr` 上。

```xml
<!-- 改前 -->
<a:bodyPr/>
<!-- 改后：声明字号仍是模板值 28pt，渲染时等比缩到 22.1pt -->
<a:bodyPr><a:normAutofit fontScale="78900" lnSpcReduction="0"/></a:bodyPr>
```

两个理由：

1. **声明字号仍是模板值** ⇒ `verify_template_fidelity.py`（R7，比的是版式/母版）与
   `verify_pptx.py` 的"标题回归版式槽位"**两条体检都照样通过**。改的是"这一页怎么显示"，不是"模板长什么样"。
2. 标题文字如果以后改短了，字号能自然恢复 —— 硬覆盖不会。

`--mode sz` 是**备选**：直接把 `sz` 写进每个 run 的 `rPr` 并加 `noAutofit`。
它是**页面级硬覆盖**，会被 `verify_pptx.py` 的"标题字号偏离版式"记一笔，只在用户明确要"字号就要变小"时用。

> `verify_pptx.py` 会为此报一条 **P1**：「标题靠 normAutofit 缩排 N 页（有效字号 X–Y pt；这些页版式原字号 Zpt）」。
> **这是预期行为，不是缺陷** —— P1 是"需人工判断"，交付说明里要主动交代"哪几页标题为单行而缩小了"。

### 5.3 哪些框算"标题"（范围定义）

范围定义只写一次，放在 `scripts/pptx_title_scope.py`，**fix 与 verify 共用** ——
两边各写一套就会"一边修 A、一边验 B"。三档：

| 档 | 范围 | 判定依据 |
|---|---|---|
| ① 标题占位符 | `p:ph type="title"` / `ctrTitle` | 永远纳入 —— 标题就是要单行 |
| ② **标题槽位**（默认纳入） | 版式里那些**提示文本 ≤24 字符、框容行数 ≤1** 的 body 槽位 | **版式自己写的提示文本**，不是槽位下标（下标换模板就失效）。本模板上正好命中：`Section Title` 的正文标题、`目录`/`Workshop index` 的分节名与序号、封面的 `Speaker`/`Date & Location` |
| ③ 副标题 | 默认**排除** | `subTitle` 折两行是设计意图（封面那行长句本来就该折） |

**不要用"框容行数 ≤1"单独当判据**：实测这样会把正文小卡片（框只够一行、文字本来就该折两行）全卷进来 —— 40 处里 34 处是误报。必须叠加"版式提示文本很短"这一条。

### 5.4 工具链

```bash
PY="${PY:-python}"        # 需已装 python-pptx / lxml / Pillow；缺模块就把它指到你的解释器
SKILL="${SKILL_DIR:-$HOME/.workbuddy/skills/pptx-polish}"
S="$SKILL/scripts"

# ① 体检（只读）：哪些标题会换行、需要缩到多少
"$PY" "$S/fix_title_oneline.py" "<工作目录>/4配色.pptx" --check --json "<工作目录>/oneline.json"

# ② 修（写 fontScale 到页面，不碰版式/母版）
"$PY" "$S/fix_title_oneline.py" "<工作目录>/4配色.pptx" \
      --out "<工作目录>/5标题单行.pptx"

# ③ 复核：让 PowerPoint 自己数行数（违规必须 = 0）
"$PY" "$S/verify_title_lines.py" "<工作目录>/5标题单行.pptx" --json "<工作目录>/lines.json"

# ④ 三层体检照旧（P0 必须为 0）
"$PY" "$S/verify_pptx.py"              "<工作目录>/5标题单行.pptx" --out "<工作目录>/verify5"
"$PY" "$S/verify_template_fidelity.py" "<工作目录>/5标题单行.pptx"
"$PY" "$S/open_test.py"                "<工作目录>/5标题单行.pptx"
```

> **⑤ 渲染肉眼确认**（不可省）：缩字号是**看得见**的改动。裁出标题带做 1:1 对照图，
> 确认"改前第二行压线 → 改后单行干净、正文位置未动"。
> 因为标题框 `anchor="ctr"` 且高度固定，缩字号**只影响标题自身**，正文与分隔线都不该移位。

### 5.5 结果判定

`fix_title_oneline.py` 每一处只有四种结论，**各自的动作不同，不要混**：

| 结论 | 含义 | 默认动作 |
|---|---|---|
| `OK` | 已经单行且缩得不多不少（误差 < 0.2%） | 不写文件 |
| `SHRINK` | 放不下（或缩得不够） | **写 `fontScale` 缩到刚好单行** |
| `STALE` | **缩过头了** —— 现有 `fontScale` 比"刚好放得下"还小（上游写死，或标题后来改短了）。它会把"合格"变成"没必要的小字" | **只报告不写**；要放大回目标字号加 `--restore-stale` |
| `HARD` | 标题里有**硬换行**（`<a:br/>` 或人工分段） | **绝不写**。缩字号救不了，必须改文字 |
| `UNRESOLVED` | 需要缩到低于下限（默认 70%，即 28pt→19.6pt）才单行 | **绝不写**。报告"要单行请把标题压到约 N 字符内" |

**基准必须是"声明字号"，不是"当前有效字号"** —— 用后者当基准永远算不出 `STALE`：
一个被缩到 50% 的标题用当前字号量当然放得下，会被误判成 `OK`，于是"缩过头"永远查不出来。

> 后两类（`HARD` / `UNRESOLVED`）的字号/文字取舍**必须交给用户**，不能自己定：
> 字号是版面档位，缩到看不清等于把合格改成不合格；标题文字受 R2 保护，不许擅改。

### 5.6 交付判定

- `verify_title_lines.py` **违规 = 0**（每个在范围内的标题都是 1 行）；
- `fix_title_oneline.py --check` 待处理 = 0（即写完之后再跑一次应报"已合规"，**幂等**）；
- `verify_pptx.py` P0 = 0、`verify_template_fidelity.py` P0 = 0、`open_test.py` 通过；
- 渲染复核过标题带：第二行不再压分隔线，且**标题框以外像素差异 = 0**（标题框 `anchor="ctr"` 且高度固定，缩字号不该牵动任何其他元素）。

### 5.7 实测（2026-09-18，Ruijie Reyee Public Wi-Fi Hotspot Solution）

| 项 | 数值 |
|---|---|
| 范围 | 42 个文本框（标题占位符 + 标题槽位） |
| 改前 | **6 处换行**（P4/P8/P18/P30/P31/P32），PowerPoint 实测均 2 行 |
| 改后 | **违规 0**，42 个全部 1 行；`--check` 复跑报"已合规 42/42"（幂等） |
| 缩放 | 28.0pt → 25.3 ~ 26.4pt（P4 最长标题 24.0pt，`fontScale=85716`） |
| 改动面 | 6 个 `ppt/slides/*.xml`，每个**只插入一个 `normAutofit` 元素**（+63 字节）；其余 **254 个部件解压后内容字节零变化** |
| 渲染复核 | 35 页里只有这 6 页有像素差异，且**标题带（y<110pt）以外差异 = 0**；标题左边缘 `34.9pt` 前后一致（0.0pt 位移） |
| 三层体检 | `verify_pptx` P0=0（P1 一条 = 缩排 6 页）／保真 P0=0 P1=0／真机打开 OK 35 页 |

> 整包体积会涨 ~13 KB（0.026%）—— 那是**全部 260 个部件按当前压缩级别重新压缩**的结果，
> 不是内容变化。内容净增只有 `6 × 63 = 378` 字节。判断"改了什么"要看**解压后内容**，不要看 zip 体积。

**为什么这几页超宽**：模板标题框文字区 `686.2pt`（`cx 700.6` − 左右内边距各 `7.2pt`），
28pt 下 P4 的「Global Hotspot: A Connectivity Business Worth Building」需要 `776.6pt`（**1.132 倍**）。
右侧 logo 起于 `759.9pt`、纵向与标题带重叠，**没有加宽空间**，只能缩。

> ⚠ **不能用 `cx`（700.6pt）当可用宽度**：那会漏掉 14.4pt 的内边距，算出的缩放比偏大、标题照旧换行。
> 可用宽度 = `cx − lIns − rIns`（未声明时取 OOXML 默认 `7.2pt + 7.2pt`）。

---

## 参考文件

| 文件 | 用途 |
|---|---|
| `README.md` | **面向人**的说明书：这是什么、依赖与环境、安装、逐步用法、仓库结构、许可 |
| `LICENSE` | **专有许可（保留所有权利）**。含"品牌资产不随本许可授予任何权利"条款 |
| `references/brand-color.md` | **Reyee 品牌色规范**（含模板实测色值与已确认口径）、单页三色规则 |
| `references/layout-patterns.md` | 版式判定 + 排列范式 + 图片排版 |
| `references/audit-checklist.md` | 五步的审计维度与交付前自检 |
| `references/report-template.md` | 各步的变更摘要模板 |

> **本 skill 面向公开发布**：文档与代码里**不写任何用户相关的绝对路径**（根目录从 `$HOME` / `SKILL_DIR` 推、
> 字体目录从 `%LOCALAPPDATA%` / `%WINDIR%` 推、外部图库从 `OneDrive` 环境变量推）。
> 新增脚本时保持这条 —— 交付前跑一次「文本文件里没有 `C:\Users\<某人>`」的扫描。
>
> **整包即完整交付物**：`assets/`（两个 `.thmx` 模板 + 311 张产品图）、`references/`（含品牌色规范）连同脚本**全部随包分发**，不需要额外准备素材。
>
> **分发打包：一条命令。**
>
> ```bash
> S="$HOME/.workbuddy/skills/pptx-polish/scripts"   # 换成你的安装目录
> "$PY" "$S/pack_skill.py" -o "dist/pptx-polish.zip" --verify
> # → 打包 + 解压到全新临时目录做 5 项功能自检，退出码 0 = 可分发
> ```
>
> 判据不是"文件在压缩包里"，而是「**解压到一个全新路径后，引用的每个相对路径都还在、都能取到**」——图库索引绝对路径、`.thmx` 引用写死绝对路径，这两个事故都是靠这条检查抓出来的。
>
> ⚠ **打包与自检必须合成一个动作，且自检必须从「产物」而不是「源目录」出发。** 实测事故：手工打的 2026-09-17 10:05 那版包只有 18 个 `.py`，而源目录已有 21 个 —— `dedupe_masters.py` / `fix_title_style.py` / `open_test.py` 整轮没发出去，而 `check_links.py` **报 PASS**（它只查"文档引用的东西在不在"，不查"有没有漏发"）。**"包比源目录旧"是静默失效**，`pack_skill.py --verify` 就是为堵这个口子。
| `scripts/deck_rules.py` | **共享判定规则的唯一事实源**：标题判定（audit 与 normalize 共用）+ 图片几何判定（`srcRect` 有效区域 / 变形 / 过采样倍率，audit 与 fix_geometry 共用）。禁止各写一套 |
| `scripts/analyze_template.py` | 解析 .thmx/.pptx 模板结构（版式、配色、字体、品牌色分布） |
| `scripts/inject_template.py` | **Step 1.1** 注入模板母版体系 + 逐页重映射版式 |
| `scripts/normalize_deck.py` | **Step 1.2** 标题归位 + 字体继承主题 |
| `scripts/audit_deck.py` | XML 级审计（标题归位、字体硬编码、单页色数、压版式装饰保留区、栅格偏差、图片变形/放大）。**几何判定统一走 `walk_shapes()` 的累积变换**，组合内形状也按绝对坐标比 |
| `scripts/dedupe_layouts.py` | **Step 0** 版式指纹分析与去重执行 |
| `scripts/size_diag.py` | 包内体积分布 / 孤立部件 / 主题与母版清点 |
| `scripts/font_diag.py` | 嵌入字体重复指纹 / 孤儿部件 / 实际 typeface 用量 |
| `scripts/check_links.py` | **分发前断链自检**：扫包内 `.md` 的相对路径引用，确认模板 / 图库 / references 都随包可达。有断链返回码 1。**它查的是"文档引用的东西在不在"，不查"有没有漏发"** —— 两者互补，别只跑一个 |
| `scripts/pack_skill.py` | **打包 + 可移植性自检**（分发前跑这一个就够）：打成单根 `pptx-polish/` 的 zip，再**解压到全新临时目录**做 **5 项**功能自检（① 断链 ② 图库索引 rebase 到解压目录 ③ 别名与裸文件名都能解析到包内 `.thmx` ④ **全量**脚本逐个 `py_compile` ⑤ **清单对账：源目录每个文件包里都得有**）。`-o` 指定输出，`--verify` 开工自检，`--keep` 保留解压目录。**语法检查与清单对账都必须扫全量，不能写死清单** —— 写死的清单在新增脚本时静默失效（2026-09-18 实测：Step 5 的三个脚本就不在旧清单里） |
| `scripts/media_diag.py` | 媒体引用可达性、仅被未引用版式占用的媒体 |
| `scripts/recolor_deck.py` | **Step 4** 配色普查（含角色分布）与品牌色替换（支持 `--only-role`）。角色共 7 类：`text` / **`title`**（标题占位符里的文字色，**不参与品牌色统一** —— 它的正确值是版式槽位定的，如内容页 `043CC1`）/ `line` / `gradient` / `shadow` / `bg` / `fill` / `other` |
| `scripts/verify_pptx.py` | 包完整性体检（链路闭合 / 悬空引用 / OOXML 元素顺序 / **content-type 类型正确性** / **标题字体·字号·颜色是否回归版式槽位**）。**它拿产物自己的版式当权威值 —— 版式被改了它照样报 P0=0**。交付前必看"标题规范"一节，不要只看 P0=0 |
| `scripts/verify_template_fidelity.py` | **R7 模板保真体检**：以模板文件（`.thmx` / 模板 `.pptx`）为**唯一事实源**，逐版式、逐形状（名称/几何/占位符槽位/背景图）、逐 run 属性（sz/b/i/u/spc/kern/latin/ea/cs/颜色）比对，另查母版 `txStyles` 与主题 `fontScheme`。P0 = 模板被改，退出码 1。默认 `--theme default`，可 `--template <路径>` 指任意模板世代。**部件名要按形状匹配（`\w*[Ss]lideMaster\d+\.xml`），不能只认裸名** —— `inject_template.py` 注入后会加 `tpl` 前缀，只认裸名会把产物数成"0 个版式、0 个母版"并报**假 P0**；主题同理，必须取 `presentation.xml.rels` 指向的那份，不能取"第一个 themeN.xml"（那会读到还没回收的旧主题，结论方向反了） |
| `scripts/fix_title_style.py` | **清页面级字体/字号覆盖**，让标题回归版式继承（`--scope title\|body\|all`）。**颜色中立**：不动 `a:solidFill`。含 `--check` 干跑。**`--overflow-shrink` 已弃用**（只认 `type="title"`、宽高写死 700.6pt/28pt，漏掉标题槽位与内边距）—— 改跑 `fix_title_oneline.py` |
| `scripts/pptx_title_scope.py` | **"哪些占位符算标题"的唯一定义**（`fix_title_oneline` 与 `verify_title_lines` 共用）。含：版式槽位解析、"标题槽位"的模板驱动判定（提示文本 ≤24 字 + 框容行数 ≤1）、字体族→本机字体文件解析、文本宽度度量。**同一个判定绝不允许存在两份实现** |
| `scripts/fix_title_oneline.py` | **Step 5 标题单行收口**：按真实字体度量算所需缩放比，把 `normAutofit fontScale` 写到**页面**（`--mode sz` 可硬改字号）。**不碰版式/母版**（R7）。四种结论：`OK` / `SHRINK`（写）／`STALE` 缩过头（只报告，`--restore-stale` 才写回）／`HARD` 硬换行与 `UNRESOLVED` 低于 `--min-scale`（默认 0.70）都**不写**。幂等 |
| `scripts/verify_title_lines.py` | **Step 5 的事实体检**：走 PowerPoint COM 读 `TextRange.Lines().Count` —— **PowerPoint 自己数出来的行数**，同时报生效字号（含 `fontScale` 折算）。范围与 fix 共用 `pptx_title_scope.py`。退出码 1 = 有标题换行。**XML 度量与渲染数墨迹都只是预测，只有这一步能下结论** |
| `scripts/open_test.py` | **PowerPoint 真机打开测试（交付前必跑）**：把文件搬到 ASCII 临时目录用 COM 打开并报页数，能抓到 `verify_pptx.py` 与 python-pptx 都漏掉的包级错误（实测 `.rels` content-type 写错即 `E_FAIL` 拒收）。退出码 1 = 有文件被拒收 |
| `scripts/dedupe_fonts.py` | **Step 1.4 嵌入字体去重**：只合并字节完全相同的 `.fntdata`（`--dry-run` 可先看省多少）。不做子集化 |
| `scripts/render_deck.py` | **渲染**逐页 PNG（Windows / PowerPoint COM）。Step 2–3 的视觉判定必须靠它复核 |
| `scripts/replace_image.py` | **Step 2 换高清图**：图片框零改动，只替换 `ppt/media` 位图数据。`--match body` 按产品本体等径对位，`--new-model 型号` 直接走图库索引取图 |
| `scripts/fix_geometry.py` | **Step 2.7 改几何**（不换素材）：`--fix-aspect` 校正显示框宽高比消变形；`--drop` 删残留形状并级联清理关系与孤儿媒体部件。`--slide N --shape-id ID` 定位，与审计报告的 `p{slide}_s{id}` 对应 |
| `scripts/product_lib.py` | **产品图库索引**：扫图库按型号建索引（`--build` / `--find` / `--grep` / `--list`）。单库/多库模式自动判定，根目录**包内优先**。索引 `assets/product_index.json`（含跨机器的 `rel` / `root_rel` / `root_key`） |
| `scripts/match_product.py` | **Step 2 认型号**：把 PPT 里的产品图与图库对上号（相关度 + 长宽比门控），产出"可自动修复清单"。闭环最后一环 |
| `assets/master/` | 模板存放目录（两个 Reyee thmx 已入库） |
| `assets/product_images/` | **随包分发的产品图库**（311 个 webp / 37.6 MB / 17 个系列）。`product_lib.find_root()` 默认命中这里，解压到任何路径都能取图 |
| `assets/product_index.json` | 产品图库索引（310 条 / 309 型号，根目录为包内 `assets/product_images/`）；`product_index_sigs_webp.npz` 是匹配用的特征缓存。每条记录的 `rel` 跨机器可移植，`path` 由 `load()` 按本机重算 |

## 二十个陷阱（踩过，别再犯）

`audit_deck.py` / `match_product.py` / `fix_geometry.py` / `product_lib.py` / `inject_template.py` / `pack_skill.py` 的早期版本有二十类错误，全部已修正。改动判定逻辑或分发资产前先读这一节：

| 陷阱 | 错在哪 | 正确做法 |
|---|---|---|
| **docstring 声明的检查项，函数体里根本没实现** | `verify_pptx.py` 头部写着"**8. 字号 / 字体 / 颜色统计（规范体检）**"，但 `check()` 里从来没有这一项 —— 它是早期写文档时的规划遗留。后果不是报错，而是**整类问题不在检测范围内**：23 页标题用 26pt `Arial`（原稿值）而非模板 28pt `Noto Sans bold`，体检全程 `P0=0` 通过。**"工具说没问题"和"工具没查"在输出上长得一模一样** | 每次改完工具，**把文件头/文档里的检查项清单与代码逐条对账**（清单项 ⇄ 实现位置），发现"写了没做"要么补实现、要么删声明。同理适用于 `audit-checklist.md` 与 `references/` 里的规则表。**推广判据：凡是"某维度从未出现在任何 P0/P1 输出里"，先怀疑它没实现，而不是怀疑它没问题** |
| **把裁切当变形** | 图片用 `srcRect` 裁切填充时，显示框比例与原图比例不符是**设计意图**。不读 `srcRect` 会报一堆假变形 | 用"有效原图区域"（扣除 l/t/r/b 千分比）与显示框比比例，偏差 > 5% 才算变形 |
| **把装饰当内容** | 栅格判定只看了宽度，把页面装饰竖条（8×44px，L=44）报成"越栅格"——而版式自带装饰条在 L=29，更靠左 | 栅格**只约束有文字的文本框 / 图片 / 表格**，纯装饰形状不参与 |
| **把近黑/近白/灰调当有彩色** | 只看 RGB 极差，`#18253A`（近黑深藏青文字）、`#F0F6FF`（近白浅底）、`#63758A`（低饱和灰蓝）全被算进"单页颜色数" | 判据加三维：极差 < 14 **或** 明度 ≤ 0.30（近黑）**或** 明度 ≥ 0.90（近白）**或** 饱和度 ≤ 0.25（灰调）→ 中性 |
| **两处各写一套判定** | `audit_deck.py` 用 60 字符阈值、`normalize_deck.py` 用 40 —— 46 字符的标题"审计报出、规范化跳过"，静默漏项 | 共用 `deck_rules.py`。**同一个判定绝不允许存在两份实现** |
| **声明包围盒 ≠ 可见墨迹** | 报"压装饰保留区"，用 XML 的 shape 包围盒判交叠。但模板 logo 的保留区框自带约 30px 上内边距，脚注文字落在空白里就被算成"压 38%"；元素重叠同样中招（文本框框大而文字只占一角） | **凡涉及位置交叠的判定，一律渲染后用图复核**（`render_deck.py`）。几何判定只作为"候选"，不直接当结论向用户陈述 |
| **用绝对像素数当"低分辨率"判据** | 原判据是"原生长边 < 480px 即低清"（`low_quality`）。333px 的图缩到 145px 显示（**2.3x 过采样**）本是清晰的，被判低清；而 401px 的图拉到 455px 显示（**1.13x**）反而漏掉 | 清晰度只取决于**过采样倍率** = 显示尺寸 / `srcRect` 之后的**有效原图区域**。`zoom > 0.75`（余量不足 1.33x）才算问题。绝对像素数说明不了任何事 |
| **拿"留白占比"当型号匹配判据** | `match_product.py` 试过用 `fill`（内容占画幅比例）过滤假匹配。但图库 PNG 是紧贴裁切的产品图，`fill` 会把产品本体算成内容 → 连 **0.999 的真匹配都被误杀** | 匹配只信**相关度分 + 长宽比**。实测真匹配的长宽比几乎一致（4.28 vs 4.31），且两库图形比例天然不同，长宽比门控有效 |
| **组合内子形状的坐标没做父级变换** | **python-pptx 不做 `grpSp` 变换**：组合内子形状的 `.left/.top/.width/.height` 返回 `<a:off>/<a:ext>` 原值，即**子坐标系**，和外部形状的绝对坐标不可比。它们拿去两两比较，就会把相距很远的元素判成完全重合。实测 P22 三个 PoE 徽标绝对位置分别在 x=147 / 571 / 145，但第 1 与第 3 个所在组合的 `chOff/chExt` **完全相同**，于是子形状坐标逐位一致 → 报成"100% 完全叠印"，还被误当成"编辑残留"差点删掉一个可见徽标；图片同样中招（p22_s44 名义 454.5px、**绝对只有 110px**，"放大 1.13x"是假的） | 绝对坐标 = `off + (child − chOff) × (ext / chExt)`，逐层累积。`walk_shapes()` 已改为 `yield (shape, transform)`，所有几何判定必须走 `rect_of(shp, tr)` / `abs_geom(shp, tr)`。**改坐标相关代码前先确认拿到的不是子坐标系值** |
| **全透明图片被当成"糊图"** | 编辑残留的空占位图（如 48×48 全是 alpha=0）被拉到 122×122 显示，被报"放大 2.54x 必然发虚"。**它根本不产生任何像素**，放大多少倍都不会糊 | 先判 alpha 是否全 0（`blank_image()`），是则报"**空图（全透明）**、编辑残留"，不再报分辨率与变形 |
| **"保持不动"的那一维被舍入悄悄改掉** | 消变形时把目标尺寸先舍到 0.1px 再换回 EMU → 说好"保持高度不变"，实测 `cy` 从 `416681` 变成 `416242`（少 0.046px）。单看像素数毫无感觉，但它违反"其他元素零改动"的承诺，也让逐像素比对失去意义 | 保持不变的那一维**直接沿用原 EMU 值**，不经过 px→EMU 往返；中间量不要提前舍入（只在显示时 round） |
| **用 python-pptx 保存来实现"只改一个数字"** | `Presentation.save()` 会**重写整包**，丢掉未建模的部件（`a:extLst` 扩展、未知关系、非标准部件），"改一个 `a:ext cx`"的实际后果是"整包重写"，且难以证明没伤到别处 | 小改动一律**字符串级定位 + 精确替换**（`fix_geometry.py` / `dedupe_fonts.py` 都是这个路子），并用「**部件级字节比对**」证明除目标片段外零变化 |
| **解析函数在另一种入参类型下静默返回 None** | `parse_src_rect()` 原先只认 lxml 元素 / dict。而 `fix_geometry.py` 走字符串级定位，传进去的是 **XML 片段字符串** → 一路走到 `except` 返回 None。"**没有 srcRect**"和"**解析失败**"返回同一个值，于是一张 `t=13740` 裁切过的图被当成未裁切，比例算成 4.82 而不是 5.59 | 解析函数要**显式覆盖所有调用方的入参类型**；"解析失败"与"确实没有"尽量不要共用一个返回值。新写一个读取函数，先在**每条调用路径**上各测一次再往下用 |
| **索引里存绝对路径，而产物要跨机器分发** | 图库索引原来存建库时的**绝对路径**，本机用得好好的；一旦随 skill 包分发出去，别人机器上 310 条全部失效（`--new-model` 直接报"索引里的文件不在了"）。同类问题：SKILL.md 正文里硬编码的 python 解释器路径 | 只要产物**可能被复制到别的机器或路径**，就存**相对包根的路径**，读取时现算绝对路径（`product_lib.rebase()` 用 `<SKILL_DIR>/<root_rel>/<rel>` 重算，消费方零改动）。同理，"索引是否过期"的判据也要用相对口径（`root_key` = `skill:assets/product_images`），用绝对路径会让**换台机器**被误判成"图库挪位"而触发重建 |
| **给 `.rels` 部件写错了 content-type** | `inject_template.py` 的 CT 补写循环用**目录名子串**判定类型（`"/slideLayouts/" in dst` → `add_content_type(..., slideLayout)`）。但版式/母版的**伴生 rels 文件就在同一目录树里**（`ppt/slideLayouts/_rels/tplSlideLayout1.xml.rels`），于是 17 个 `.rels` 被盖章成 `slideLayout+xml` / `slideMaster+xml`。**PowerPoint 直接 `HRESULT E_FAIL` 拒收整包**；而 `python-pptx` 从不校验 content-type、`verify_pptx.py` 的"覆盖率"检查（有 Override 就算覆盖）也报 P0=0 —— 三重假阴性，白排查了 CT 首条目、孤立部件、XML 语义对比一大圈 | 判目录名一律用 `posixpath.dirname(dst)` **全等**，不用子串包含；`.rels` 单独一线短路走 `Default rels`，不进目录分支。同时在 `inject_template.py` 里加**产物自检 `_package_self_check()`**（不通过就抛，不出货），并在 `verify_pptx.py` 增 2b 项：**`Overrides` 里凡 `.rels` 的 content-type ≠ `application/vnd.openxmlformats-package.relationships+xml` 即 P0**。交付前再跑 `open_test.py` 真机开一次 |
| **自检脚本把检查对象写死成清单** | `pack_skill.py` 的第 4 项自检原先写死 6 个脚本名逐个 `py_compile`。新增脚本**不在清单里就永远不被检查** —— 2026-09-18 加 Step 5 时，`pptx_title_scope.py` / `fix_title_oneline.py` / `verify_title_lines.py` 三个脚本压根没进自检，语法错了也会打印"OK"。**这是本表第 1 条（"写了没做"）的同一类错误，只是载体从 docstring 换成了硬编码常量** | 自检的**检查对象要扫全量、从产物上枚举**（`sorted(f for f in os.listdir(s) if f.endswith('.py'))`），不写死名单。再加一项**清单对账**：源目录有的文件，包里必须逐一都有（`src_files - pkg_files` 必须为空）—— 这才是"漏发"的正面判据。**判据通用化：凡是"枚举型常量"出现在自检/校验代码里，就要问一句"新增一项时它会自动覆盖吗"** |
| **颜色只认 `srgbClr`/`schemeClr`，漏掉 `prstClr`** | `audit_deck.py` / `recolor_deck.py`（以及当时那个一次性的白字普查脚本）的颜色采集都只匹配 `<a:srgbClr val>` 与 `<a:schemeClr val>`。而原稿把封面、封底的标题/副标题/作者/日期/网址全写成 **`<a:prstClr val="white"/>`（预设色）** —— 共 21 处白字，几个工具全都看不见：白字普查报"没有白字"，颜色普查不计数，`recolor_deck.py --map` 也改不动。换成浅色模板后这些字整片消失（白字压浅底），排查时还以为工具没跑对 | 颜色匹配要覆盖 OOXML 的**四种**写法：`srgbClr`、`schemeClr`、`prstClr`（预设色名，须有一张 `white/black/gray…` → HEX 的对照表）、以及 `sysClr`。**普查口径不全 = 结论不可信**。本包实测 `prstClr` 共 100 处（white 21 / black 79）|

修正后判定数从 130 项降到 **58 项** —— **删掉的是噪音，留下的是真问题**。

### 五个"换模板"专属陷阱（2026-09-17，T3T4 ISP 实战，代价最大）

| 陷阱 | 错在哪 | 正确做法 |
|---|---|---|
| **为"避免与原稿冲突"删掉版式自带装饰** | 换完模板发现原稿内容压到模板装饰（标题左侧绿短横被原稿同款短横叠上、标题下分割线横穿原稿副标题行、封底 `THANKS!` 与原稿封底文字重叠），于是**把模板那三个形状从版式里删了**。用户看到的就是"**你说套了模板，可模板的标志性元素全没了**"——标题左侧的绿矩形、封底 Thanks 都是模板的设计语言，删掉等于没套。**方向做反了** | **版式的装饰一个都不许删**（它属 Step 3「保留区」）。冲突的正确处置是让**原稿内容**让位：原稿里的重复短横删原稿的、文字移出版式保留区、封底文字改为不压 `THANKS!` 的位置。**判定"某个装饰该不该在"的唯一依据是 `.thmx` 原版**：按 `(cNvPr@name, 形状类型)` 做 key，逐形状点名比对 `.thmx` 的 layout 与本包的 layout，少一个就是错，不要凭"看着像重复"下手。宁可先不动、把冲突列给用户，也不要单方面删模板 |
| **只 glob `ppt/slideLayouts/*.xml` 来审版式** | 用户用 PowerPoint 重新套过一次模板后，包里会有 **32 个 slideLayout**：原来的 `tplSlideLayout1-16`（PowerPoint 另存时改名为 `slideLayout1-16`）**仍挂在 `slideMaster1` 上并且 `slideMaster1` 仍列在 `presentation.xml` 的 `sldMasterIdLst` 里**；同时 PowerPoint 新建 `slideMaster2` + `slideLayout17-32`（形状完整），**34 页全部迁到 master2**。于是"从 master 读到的 16 个版式"和"页面实际用的 16 个版式"是**两批不同的东西**：前者缺绿条/缺 THANKS，后者完整。PowerPoint 的"版式"面板里排在最前、用户随手能选到的正是**残缺的那批** | 审版式必须**分母版**：读 `presentation.xml` 的 `sldMasterIdLst` + 每个 master 的 rels + `sldLayoutIdLst`，再合上"每页实际引用哪个版式"。`ppt/slideMasters/` 下有 2 个 master、`slideLayouts/` 下有 32 个 layout 就是这个症状。处置：**保留被引用的 master，把未被任何页引用、且不再必需的孤儿 master + 它的全部 layout + 它的专属 theme/tags + 对应 rels/CT Override 一起删掉**（`scripts/dedupe_masters.py`）。否则用户下次一"重设版式"或换版式，绿条与 Thanks 立刻打回原形 |
| **把"重设版式"（Reset Slide）当成纯 UI 动作，不知道它在 OOXML 层做什么** | 用户在 PowerPoint 里点"重设版式"后，占位符几何、甚至占位符的有无都变了，但如果不知道它的语义，就解释不了现象、也复现不了。实测它等价于：**① 删掉 slide 上占位符自己的 `<a:xfrm>`（位置/尺寸回归版式）；② 给"版式有槽位、页上没有实例"的占位符补一个空实例**（实测 P15 被补进一个空的 `body idx=1`，其余 24 页没有 → 不一致）；**③ 版式里没有对应槽位的占位符**（本模板 16 个版式全都没有 `sldNum` 槽位），`off.x` 被写成 **0** —— 页码直接贴到画布左边缘。用户明确要求"每次修订后做一次重设版式"时，这三条会**反复**发生 | ① 是**要的效果**（标题位置统一，不再"上上下下"），保留。② 要**清掉**：重设后全量扫一遍 slide，删掉"文本为空、无图片"的 body 占位符；**例外：`目录` / `Workshop index` 版式自带的留空行**（本稿只有 4 个章节，第 5 行 `body idx=25/26` 是有意留空）**不能删**。③ 要**把 `off.x` 改回去**：本包页码正确位置 = 原稿墨迹的 `x=236220`（24.8px），`y=6153150`。**重设版式之后必须再跑一次这套收口**，所以它值得固化成一个脚本而不是每次手改 |

> **修 bug 的顺序很重要**：上表第 4、8 两条是"同一个判定两处实现 / 两套坐标系"性质的错误，**会静默污染所有下游判定**。遇到"某页结论看起来不合常理"时，先怀疑坐标系和事实源，而不是先怀疑页面。

#### 第 4 条：删了旧母版，却把「演示文稿级主题」留在旧母版的主题上

| 陷阱 | 错在哪 | 正确做法 |
|---|---|---|
| **按"引用计数"判主题该不该删／该不该改指** | 换模板后包里有两个主题：`theme1.xml`（名 **`2025 Reyee`**，属旧 `slideMaster1`）、`theme2.xml`（名 **`Reyee-Eng-2025`**，属新 `slideMaster2`）。删掉孤儿 `slideMaster1` 后，`presentation.xml.rels` 的**演示文稿级主题**仍指向 `theme1.xml`。而 `theme1.xml` 此刻被 **旧母版 + presentation 同时引用 → 引用计数 = 2**，于是"只删引用计数为 0"的孤儿扫描把它判成"有人用"：**既不删、也不重指**。后果是主题库里残留一个过期主题，用户新建形状/取色/取字体仍继承旧模板的血统 —— 又一次"看着换过模板但没真换"。**判定必须按血统（这个 theme 挂在被删母版下），不能按引用计数** | 删孤儿母版时同步记下 `dead_themes`（被删母版及其版式引用的 theme）与 `live_theme`（存活母版引用的 theme）；`presentation.xml.rels` 的主题若 ∈ `dead_themes` 且 ≠ `live_theme`，就**改指 `live_theme`**，并让孤儿扫描**跳过该 `.rels`**（它即将被改写，旧引用不算数），从而把旧 theme 一并回收。`dedupe_masters.py` 已内置。**验证口径**：产物里 `presentation.xml.rels` 的主题与 `slideMasterN.xml.rels` 的主题必须是同一个文件 |
| **`resolve(host, target)` 传了 `.rels` 路径当宿主** | 拼相对路径时宿主应是**宿主部件**（`ppt/presentation.xml`），传成关系文件（`ppt/_rels/presentation.xml.rels`）后，`dirname` 得到 `ppt/_rels`，于是 `theme/theme1.xml` 被解析成 **`ppt/_rels/theme/theme1.xml`** —— 与真实部件名永不相等，判定**永远不命中且不报错**（静默失效）。同一处 `posixpath.relpath(target, posixpath.dirname(PRES_RELS))` 也会把改写后的 Target 写成 `../theme/theme2.xml`（多一级）。这类错误在"症状是没做事"时最难发现 | 一律 `resolve(主机部件, target)` / `relpath(target, dirname(主机部件))`。**`build_referenced(..., skip=)` 按宿主部件比对**，所以 skip 里也要塞 `ppt/presentation.xml` 而不是 `ppt/_rels/presentation.xml.rels`。规则化：**全脚本只允许一种"由 part 推 rels / 由 rels 推 part"的换算**（`part_rels_path()` / `host_of_rels()`），任何手写 `+ ".rels"` 或 `dirname()` 都算 bug |

> 本条的发现方式值得记：工具产出与手工产物**判定逐字相同**（P0=0、悬空=0、母版唯一、16 版式），但**包部件集合差一个**（工具多留 `theme1.xml`、手工版反而漏删一个 2KB 的 `oleObject1.bin`）。是"逐部件字节比对 + 部件集合差集"把差异逼出来的 —— 只看 `verify_pptx` 的结论会以为两者等价。**结论相同 ≠ 产物相同**。

#### 第 5 条：体检只查"包结构 / 占位符 / 几何"，**从不比对字体族与字号**（检测维度缺失）

| 陷阱 | 错在哪 | 正确做法 |
|---|---|---|
| **`verify_pptx.py` 的 docstring 写着"8. 字号 / 字体 / 颜色统计（规范体检）"，函数体里根本没有这一项** | 旧版体检的检查维度只有：zip 完整性 / Content_Types / 悬空引用 / 链路闭合 / OOXML 元素顺序 / 占位符能否被版式承接 / 几何越界。**"页面级 `sz`/`latin` 是否偏离版式继承值"这一维度完全不存在**。于是原稿把 **26pt `Arial`**（`b="1"`）写进每一页标题时，体检报 `P0=0`、`open_test` 通过、渲染也正常 —— 因为 26pt 本身合法，**只是不是模板的 28pt**。用户点出 5 页才发现**全稿 28 个标题页里 23 页偏离**。这类"结构全对、观感不对"的问题，体检不覆盖就永远查不出来 | 体检必须显式覆盖**字体继承链**。`verify_pptx.py` 已补第 8 项（`title_style_chain()`），逐页算生效字号/字体/颜色并与**版式槽位**比对，P1 报「标题字体/字号偏离版式设定 N 页」+ 覆盖来自哪一级（`slide:lstStyle` / `slide:run/rPr` …）。**交付前必看这一项，不要只看 P0**。 |
| **经验盲区：只修用户点名的那几页** | 用户报"11、12、28、29、30 页标题字体大小不对"。实测这 5 页**确实都不对**（P11/P28/P29 = 26pt `Arial`；P12/P30 = 24pt），但同样是 26pt `Arial` 的还有 **18 页**。若只修点名页，剩下 18 页照样是"没套上模板"。反过来，用户点名页里 P11/P28/P29 **颜色反而是对的**（`043CC1` = 模板值），真正颜色错的是未被点名的 21 页 —— 即**用户能看见症状、看不见成因，主观定位必然只覆盖一部分** | 拿到用户点名的页当**线索**而不是**范围**。先做**全量普查**（`verify_pptx.py` 的第 8 项 `title_style_chain()` 会逐页算生效字号/字体/颜色并与版式槽位比对，就是这份清单），拿到 N 页清单后再动手，交付时把"你点了 5 页、实际 23 页"这个差值明确讲出来 |
| **判"符合模板"只看颜色** | 上一轮 recolor 把 20+ 页标题从 `043CC1` 改成了 `0055CD`，于是"颜色统一了"看起来收口了；但 **`.thmx` 里 `标题和文本` 槽位标题色就是 `043CC1`**（`0055CD` 是**封面** `ctrTitle` 色 + 装饰形状色）。改色把"合格"改成了"不合格"，而且改得不彻底（P11/12/28/29/30/33 漏改，仍是 `043CC1`）→ 出现 **同版式下两种标题色** | 判"符合模板"必须**逐维度对版式槽位**（字号 / 字体 / 颜色 **三项分开**）。内容页标题色 = `043CC1`、封面色 = `0055CD`，**不要用品牌主色去覆盖标题专用色**。`recolor_deck.py` 按角色分治（Step 4.3）：`title` 角色不参与"统一成品牌色"的映射 |

**修复手法（`scripts/fix_title_style.py`）**：**颜色中立**地删掉页面级 `sz`/`b`/`latin`/`ea` 覆盖，让文字回归版式继承；`a:solidFill` **保留不动**（颜色是另一个维度，不在本步决策）。

```bash
PY="${PY:-python}"        # 需已装 python-pptx / lxml / Pillow；缺模块就把它指到你的解释器
SKILL="${SKILL_DIR:-$HOME/.workbuddy/skills/pptx-polish}"
S="$SKILL/scripts"
"$PY" "$S/fix_title_style.py" "<工作目录>/4配色.pptx" --out-deck "<工作目录>/4b标题回归.pptx" --scope title
# → 23 页清掉页面级 sz/字体覆盖，标题回归版式 28.0pt Noto Sans bold
# 标题太长放不下 → 交给 Step 5（--overflow-shrink 已弃用）：
"$PY" "$S/fix_title_oneline.py" "<工作目录>/4b标题回归.pptx" --out "<工作目录>/5标题单行.pptx"
"$PY" "$S/verify_title_lines.py" "<工作目录>/5标题单行.pptx"                  # PowerPoint 自己数行数，必须 0 违规
"$PY" "$S/verify_pptx.py" "<工作目录>/5标题单行.pptx" --out "<工作目录>/verify"  # 再看第 8 项
```

> **它清的是什么**：`FONT_ATTRS = ('sz','b','i','kern','spc','baseline','cap','strike','u')` + `FONT_TAGS = ('latin','ea','cs','sym')`，作用在 `txBody` 里**所有** `a:defRPr` / `a:rPr` / `a:endParaRPr` 上（`lxml.iter()` **只吃 tag、不吃路径**，所以 `tb.iter('{ns}defRPr')` 一次就能同时覆盖 `lstStyle` 与 `pPr` 下的）。
> **`--drop-lst-style` 是激进档，默认别用**：整块丢弃 `lstStyle` 会连带把 `fill` 交给版式，等于顺手改了颜色。

**`normAutofit` / 标题单行的六个陷阱（累计踩实）**：

| 陷阱 | 现象 | 正确做法 |
|---|---|---|
| **在版式上放空的 `<a:normAutofit/>`** | 想让"标题太长就自动缩"，于是给版式的 `bodyPr` 塞一个空 `normAutofit`。**渲染证实无效** —— P12/P30 依然两行 | `fontScale` 必须写在**页面**上，且是**预先算好的**值。PowerPoint 只在**用户编辑**该文本框时才计算并写回，`无头改包`不会触发 |
| **`fontScale` 算成 `box_pt / text_w_pt`（恰好等于框宽）** | 实测 P12 仍换行。因为度量与 PowerPoint 的实际断行有亚像素差 | **留安全余量**：`sc = (box_pt / w) * (1 - margin)`，`margin = 0.03` 起步。另：判定要用严格 `if sc >= 1.0: continue`，`0.999` 也要处理（有页只超 0.4pt） |
| **以为"标题框可以加宽"** | 想靠加宽标题框避免换行。实测版式「标题和文本」标题框 `48.1..748.7pt`，而右侧 logo `图片 6` 起于 **759.9pt**、且纵向与标题带重叠（`45.3..72.2pt`）→ **完全没有加宽空间** | 只能缩排。并且注意**两行 28pt 会穿过分隔线**：标题框高仅 `50.5pt`（`33.5..84.0`），两行 28pt 需 ≈`67.2pt`，`anchor="ctr"` 会上下各溢出 8.35pt → 越过 `85..87pt` 的 `Image 21` 分隔线。**口径已于 2026-09-18 定案：单行优先，放不下就缩字号**（见 Step 5）。只在缩到低于下限（70%）时才回来找用户 |
| **拿 `cx` 当可用宽度** | 用标题框 `cx 700.6pt` 算缩放比 → 漏掉左右内边距 `各 7.2pt`，可用宽度实际只有 **686.2pt**。缩放比算大 ⇒ 写下去的 `fontScale` 不够 ⇒ **标题照旧换行**，而且看起来"已经缩过了"更难排查 | 可用宽度 = `cx − lIns − rIns`，未声明时取 OOXML 默认（`lIns=rIns=91440 EMU=7.2pt`）。垂直同理（`tIns=bIns=45720 EMU=3.6pt`），`pptx_title_scope.insets_of()` 统一处理 |
| **用"框容行数 ≤1"单独判"这框该放一行"** | 想自动找出"设计上只放一行的框"。但正文小卡片（如统计图下面那行注释）框也只够一行、文字本来就该折两行。实测 40 处命中有 **34 处是误报**，会把正文一起缩掉 | 必须叠加**版式侧**的条件：该槽位在**版式里写的提示文本**很短（≤24 字、无硬换行）。提示文本是模板作者留下的"这里放多少字"的说明，比几何更可靠。见 `pptx_title_scope.heading_slots` |
| **以为 PowerPoint 的占位符类型常量是 `title=13`** | 写 `PlaceholderFormat.Type` 判定时按 13/3/17 写（记成了别的枚举），于是真实标题全被当成 `body`，**"标题是否换行"这项判定等于全空** | `PpPlaceholderType`：**`ppPlaceholderTitle = 1`**、`ppPlaceholderCenterTitle = 3`、`ppPlaceholderVerticalTitle = 5`、`ppPlaceholderSubtitle = 4`、`ppPlaceholderBody = 2`。**枚举值写完要拿真文件反查一次**（打印每页每个形状的 Name + Type 对照 XML），别凭记忆 |
| **判"要不要缩"时拿"当前有效字号"当基准** | 想用"缩完之后还放得下吗"来判断，于是用 `size × fontScale` 去量文字宽度。结果：一个被缩到 50% 的标题**当然放得下** ⇒ 判成 `OK` ⇒ **"缩过头"永远查不出来**（标题改短了、上游写死了小 `fontScale`，都会永久留在小字上）。实测把 P4 的 `fontScale` 改成 `50000`，脚本报"已合规 42/42" | 基准必须是**声明字号**：先按 `size` 算出"刚好单行需要的 `fontScale`"，再拿它跟现有的比 —— `现有 > 目标` ⇒ 还要缩（`SHRINK`）；`现有 < 目标` ⇒ **缩过头（`STALE`）**。另：比"缩过头"更隐蔽的是**判等比用的容差**，`|现有−目标| ≤ 0.2%` 才算达标，否则每次复跑都会报一次 |

### 已用渲染证实的实例（2026-09-16，BYOD Office Network v10）

| 审计结论 | 渲染复核结果 |
|---|---|
| 「压装饰保留区」10 页（P5 报 38%、P26 报 15%） | ❌ **全部误报**。红框是模板保留区，自带约 30px 上内边距；脚注文字正落在那段空白里，与 logo 之间有 8px 以上净空 |
| 「PoE × PoE 100% 重合」「PoE × 产品名 47%」（P22） | ❌ **全部误报**（2026-09-17 复查修正）。页面上就是 **3 个** PoE 徽标，绝对位置分别在 (147,478) / (571,531) / (145,585)，互不重叠。根因是**组合内子形状用了子坐标系**：第 1、3 个徽标所在组合的 `chOff/chExt` 完全相同，子形状坐标逐位一致 → 报 100%；47% 则是子坐标与绝对坐标混比。**"两个标签完全叠印、删掉零风险"这个结论已撤回——按它删会真的删掉一个可见徽标** |
| 「p22_s44 放大 1.13x」（P22） | ❌ 误报，同一根因。名义 454.5×255.3 是子坐标值，**绝对只有 110.1×64.6**，过采样 0.27x，很清晰 |
| 「p25_s96 放大 2.54x」（P25） | ❌ 数值真但结论错：那张图是 48×48 **全透明空图**（2304 像素 alpha 全 0）。修正组合坐标后才暴露出来 → 已改判"空图（编辑残留）"。**→ 已删（2026-09-17），删除后 30 页渲染逐像素比对，此页差异为 0** ✅ |
| 「p22_s58 图片偏 27.5%」 | ✅ 真。2359×489 原图、`srcRect t=13740` → 有效区域比例 5.59，显示框 7.13，交换机被横向拉长。**→ 已消变形（2026-09-17）：`--fix-aspect` 把宽 311.5 → 244.7，高度与左上角不动，变形 27.3% → 0.1%** ✅ |
| 「p22_s46 图片偏 10%」 | ✅ 数字真，属边界值 |
| 「p23_s73 放大 1.67 倍」 | ✅ 真。产品图原生仅 78×62 px，糊得很明显（同页另两个 AP 清晰）。**已用 `replace_image.py` 换高清素材，机身等径修复** |
| 「P28 放大 1.55 倍 / P29 1.23 倍」 | ✅ 数字真，但**不是产品图**——渲染复核后确认是案例实拍照片（办公室、机柜、前台壁画）。**产品图库救不了这类**，只能重拍或换现场照。`match_product.py` 报"认不出"是正确行为 |

**结论**：图片类的判定（`srcRect` / `zoom`）已经可信；**位置交叠类的判定必须渲染复核**；**"图糊了"要先分清是不是产品图**——是产品图才谈"按型号换高清"，实拍照片跟型号无关；**改完几何类改动要用"差异必须落在目标矩形内"来证明没伤到别处**（见 Step 2.7）。

#### 第 6 类：模板本体被改写 —— **R7 的由来**（2026-09-18，Public Wi-Fi Hotspot）

| 陷阱 | 错在哪 | 正确做法 |
|---|---|---|
| **把"改版式"当成"套模板"** | 用户说的是"用模板格式去改 PPT"，落到执行上变成了"把 PPT 的现状写进版式"。实测产物（35 页）与 `.thmx` 的差异：`Section Title` 版式的 `01` 占位符 `<a:defRPr sz="13800" i="1">` → `sz="13800" b="0" i="0"`（**斜体被取消**）；同版式 `Section title` 行 `b="1"` → `b="0" i="0"`；`<p:ph type="body" sz="quarter" ...>` 的 `sz="quarter"` 被删；`Noto Sans` → `Noto Sans Black`/`Noto Sans Medium`（连 `panose/pitchFamily/charset` 一起丢）；`目录` 版式 `文本占位符 6` 的 `i="1"` → `i="0"`、`ea 微软雅黑` → `Noto Sans bold`；主题从 `Reyee` 换成 `ChatGPT`；母版被清空（模板里有 `对象 4`）且 `titleStyle` 从 `2963pt 微软雅黑` 变成 `4400pt +mj-lt`；16 个版式只剩 8 个。**用户第一眼就看出来了**："模板被篡改了。" | 版式/母版/主题**只读**（R7）。要改 PPT 就改页面；页面上的覆盖会盖掉版式，这才是"用模板格式"的正确姿势。**判据只有一个**：产物版式逐形状、逐 run 属性等于模板，对不上就是错 —— 不要用"这样更好看"给自己找台阶 |
| **体检只查"页面 vs 版式"，版式被改了照样 P0=0** | `verify_pptx.py` 的权威值是**产物自己的版式槽位**。版式被改写后，页面"回归"到一个已经被改过的版式，体检报"标题已回归模板 ✅、P0=0"。这是陷阱 1「工具说没问题 ≠ 工具没查」的第二次复发，只是抬高了一层：从"某维度没实现"变成"权威值取错了对象" | 权威值必须是**模板文件**，不是产物。新增 `verify_template_fidelity.py`：把 `.thmx`（或模板 `.pptx`）当唯一事实源，逐版式/逐形状/逐 run 属性比对，P0 非 0 就不许交付。同一份产物：`verify_pptx.py` P0=0、`verify_template_fidelity.py` P0=26 |
| **默认行为本身就在改模板** | `recolor_deck.py` 的 `SCAN_DIRS` 原本含 `ppt/slideLayouts/` + `ppt/slideMasters/` —— Step 4 一跑，模板本体的颜色就被改了；`fix_title_style.py --unify-title-color` 还专门补上"recolor 常漏掉版式与母版这两处"。也就是说：**流程默认动作 = 改模板**，而红线表里没有任何一条禁止它 | 默认范围收窄到页面（`ppt/slides/` `ppt/notesSlides/`）；版式/母版要显式 `--include-template` / `--allow-template-write` 才动，并在报告里列清"模板里也有这些色值、默认不改"。**"改了会更容易交付"不构成改模板的理由** |
| **修复 R7 时新写的体检工具自己带着假阴性** | 新加的 `verify_template_fidelity.py` 只匹配裸名 `slideMaster\d+\.xml` / `slideLayout\d+\.xml`，而 `inject_template.py` 注入后会加 `tpl` 前缀 → 它把注入产物数成"**产物版式 0、母版 0**"，然后逐条报"模板里的 16 个版式产物都没有"= **P0 假阳性**。同时主题取"第一个 `themeN.xml`"，读到的是**还没回收的旧主题**，"主题没换干净"被误报成"主题被换掉"。**一次写错的检测比没有检测更糟**：它会让你去"修"一个本来正确的东西 | 新写任何"权威值来自另一个文件"的比对工具，**第一件事是用三个已知答案的输入校准**：① 模板自身（应全绿）② 明确损坏的样本（应报出已知条数）③ 修好后的产物（应全绿）。本工具校准结果：模板 0/0、受损原件 **P0=26 / P1=37**、注入产物 **0/0**。校准不通过就不许拿来判断 |
| **文件名带前缀的东西，一律不能按"裸名 + 前缀"去匹配或判类型** | 同一类错误的两次复发：`verify_template_fidelity.py` 认不出 `tplSlideLayout1.xml`；主题判定写 `startswith("theme")` 而实际叫 `tplTheme1.xml`；更早还有 `inject_template.py` 用目录名**子串**判 content-type，把 `slideLayouts/_rels/*.rels` 也盖章成 `slideLayout+xml`，PowerPoint 直接拒收 | 一律按**路径形状**匹配（`/theme/[^/]*[Tt]heme\d+\.xml$`、`/\w*[Ss]lideMaster\d+\.xml$`），不按文件名前缀、不用子串包含。判"这个目标是不是我这类部件"时，看**它自己的类型/路径位置**，不看它叫得像我什么 |

> **这次为什么难查**：产物是 PowerPoint for Mac 存的，包里没有 `tpl` 前缀、没有中间产物，
> 版式数量（8）与模板（16）不一致。所以**不要靠"有没有 tpl 前缀"判断套没套过模板** ——
> 直接跑 `verify_template_fidelity.py`，让差异自己说话。
> 顺带记一条：`b="0" i="0"` 这种"显式写死非粗非斜"的补写，在**任何**被 PowerPoint for Mac 重存过的包里都会大量出现，
> 本身不等于篡改；只有**与模板值不一致**时才算（本包 `i` 是 `1 → 0`，模板值非 0，所以是篡改）。

## 版本与更新

这个 skill 会持续迭代，**别只留一份拷贝就不再管它**。仓库根目录的 `VERSION` 是版本号唯一来源。

| 想干什么 | 命令 |
|---|---|
| 查自己这份是不是最新的（只读） | `"$PY" "$S/update_skill.py" --check` ｜ 退出码 `2` = 有新版 |
| 更新到最新 | `"$PY" "$S/update_skill.py"` |
| 先看会写哪些文件 | `"$PY" "$S/update_skill.py" --dry-run` |
| 网络要走代理 | 上面任意一条加 `--proxy http://127.0.0.1:7897` |

- **目录里有 `.git`** → 走 `git fetch` + 快进合并。**工作区有未提交改动、或本地已和远端分叉时会拒绝更新**
  （不静默覆盖别人的改动）；`--json` 给机器读。
- **没有 `.git`（zip / 拷贝安装）** → 下载最新 main，逐文件比 sha256 后同步；会逐条列出被覆盖且有变化的文件，
  且**不删除**本地多出来的文件。
- **`--check` 的退出码是约定**：`0` 最新 ｜ `1` 出错 ｜ `2` 有新版 —— 可以直接挂进脚本或定时任务判断。
- **更新完必须重启 WorkBuddy（或新开对话）才生效**：skill 是启动时会话加载的。
- 维护者侧：`git commit` + `git push` 之后别人才拉得到。**不存在"自动实时同步"** —— skill 是本地文件夹，
  更新永远是"拉"（pull），没有"推"（push）。所以给别人的建议是：**用之前顺手跑一次 `--check`**。

---

## 依赖与环境（已实测）

| 依赖 | 状态 |
|---|---|
| `python-pptx` 1.0.2 + `lxml` + `Pillow` | 装在**你自己的** Python 环境里即可（见 README「安装」）。脚本不依赖任何固定安装位置；把解释器路径给 `$PY` 就行 |
| `OOXML` 包操作 | 用 `zipfile` 直接读写 pptx / thmx 内部部件（换主题必须走这一层） |
| `slidep` | 本流程**不使用**。实测其 `screenshot` 需要 editor_sdk 里已打开的文档，喂外部 pptx 直接报 `presentation is not open`；`validate` 只吃 DSL |
| **渲染验证** | 见下节。**不要想当然地说"本机没有渲染能力"——必须先按探测顺序实测** |
| **验证边界（三层，缺一不可）** | **python-pptx 能打开 ≠ `verify_pptx.py` P0=0 ≠ PowerPoint 能打开**。顺序：① `verify_pptx.py`（覆盖 / 悬空 / 元素顺序 / content-type 正确性）→ ② **`open_test.py` 真机打开** → ③ 渲染逐页看图。2026-09-17 实测踩过一次**前两层全过、PowerPoint 仍 E_FAIL** 的漏网（`.rels` 的 content-type 被写成 `slideLayout+xml`），白排查一大圈。**第 ② 层不可省，换模板（Step 1.1）之后尤其必须跑** |
| **PowerPoint COM（Step 5 必需）** | `verify_title_lines.py` 靠 `TextRange.Lines().Count` 数行数 —— **没有 PowerPoint 就没法下"标题是单行"的结论**。此时必须退到明确口径：只报"XML 度量预测"，**不许声称"已确认单行"**。脚本在 COM 不可用时退出码 1 并直接说明这一点，不会静默给通过 |
| **字体文件（Step 5 度量用）** | `find_font()` 在**用户字体目录**（`%LOCALAPPDATA%\Microsoft\Windows\Fonts`，后装的 Noto 在这里）与**系统字体目录**（`%WINDIR%\Fonts`）里按文件名找，顺序为「文件名优先、目录其次」—— 目录现算，**不写死任何人的路径**。找不到时脚本退化为按字宽估算并把余量翻倍，同时在报告里标记"度量不精确"，**不要把它当精确结果向用户交代**。要用别的字体加 `--font-map "noto sans=<你的路径>.ttf"` |
| **产品图库** | **随包分发**在 `assets/product_images/`（单库模式；`--root` 或 `RUIJIE_PIC_ROOT` 可覆盖，指向 `…/产品图片` 即切多库）。索引 `assets/product_index.json`（310 条 / 309 型号）+ 特征缓存 `assets/product_index_sigs_webp.npz`。索引只存 `rel`（跨机器）+ `root_rel`，**不存绝对路径**；`load()` 按 `root_key` 判断是否重建、并 `rebase()` 把 `path` 现算成本机位置。外部图库默认按 `OneDrive` / `OneDriveConsumer` / `OneDriveCommercial` 环境变量推导（`<OneDrive>/PIC/产品图片`，子路径见 `PIC_SUBPATH`），**不写死盘符**。**改图库目录名会让 lib 变化 → 特征缓存文件名失配 → 310 张图全部重算特征**，所以包内目录名固定 `product_images` |

本机 shell 的 coreutils（`ls`/`head`/`tail`）不在 PATH，文件操作走 Python 或专用工具。

---

## 渲染验证（怎样"看到"页面）

改完 PPT 要能**看到**页面，否则 Step 2 配图与 Step 3 排版只能靠数值猜。三条路，优先级从高到低：

| 优先级 | 方案 | 前提 | 说明 |
|---|---|---|---|
| **1** | **PowerPoint COM 导出**（`scripts/render_deck.py`） | 装了 Microsoft Office（`POWERPNT.EXE` + 注册表有效） | **保真度最高**——就是 PowerPoint 自己渲染 |
| 2 | LibreOffice headless | `soffice.exe` | `soffice --headless --convert-to pdf`。跨平台可用，但对本模板的**图片填充背景**（`blipFill` 底图）与嵌入字体还原不如 PowerPoint |
| 3 | `slidep` | editor_sdk 里已打开该文档 | 只对已打开的文档有效；**不作为通用渲染手段** |

```bash
"$PY" "$S/render_deck.py" "<原件>" --out "<工作目录>/render"          # 全篇 2560x1440
"$PY" "$S/render_deck.py" "<原件>" --out "<dir>" --pages 5,22,26     # 只渲染指定页
"$PY" "$S/render_deck.py" "<原件>" --out "<dir>" --pdf               # 同时出 PDF
```

**输出**：`render/slide-01.png … slide-NN.png`（默认 2560×1440，即版心 1280×720 的 2 倍）、可选 `deck.pdf`、`render.log`。

**PowerPoint COM 的三个坑**（`render_deck.py` 已固化处理）：

| 坑 | 现象 | 正确做法 |
|---|---|---|
| `Application.Visible = False` **不被允许** | 报 `Hiding the application window is not allowed`（与 Word/Excel 不同） | 用 `Presentations.Open(..., WithWindow:=msoFalse)` 打开，不要去设 `Visible` |
| `Presentation.Export` 生成中文文件名 | 中文版 Office 输出「幻灯片1.PNG」而非 `slide-1.png` | 用**逐页** `Slide.Item(i).Export(path, "PNG", w, h)` |
| 非 ASCII 路径被编码破坏 | PowerShell 脚本里含中文路径时渲染失败 | 先把源文件复制到 ASCII 临时目录（`render_deck.py` 内部已做），或别写 `.ps1`、直接用 Python 调 COM |

### 何时必须渲染（不要省这一步）

| 场景 | 必须渲染 |
|---|---|
| Step 2 判断"图配不配文字""糊不糊" | ✅ |
| Step 3 判断"挤不挤""排列顺不顺" | ✅ |
| 审计报出「压装饰保留区」「元素重叠」 | ✅ **复核后再向用户下结论** |
| 确认字体是否真的回退 | ✅ |
| Step 0 / 1 / 4（纯数值） | ❌ 可省 |

### 探测顺序（别只查一个就下结论）

**已经踩过的坑（2026-09-16）**：只探了 LibreOffice 与 poppler，没探 PowerPoint，就对外说"本机没有渲染能力" —— 而那台机器实际装着 **Office LTSC ProPlus 2024**（16.0.17932）。**先探 PowerPoint，再探别的。**

```python
import glob
print('PowerPoint:', glob.glob(r'C:\Program Files\Microsoft Office\root\Office*\POWERPNT.EXE'))
print('WPS       :', glob.glob(r'C:\Program Files\WPS Office\*\office6\wpp.exe'))
print('soffice   :', glob.glob(r'C:\Program Files*\LibreOffice\program\soffice.exe'))
print('poppler   :', glob.glob(r'C:\Program Files\poppler*\bin\pdftoppm.exe'))
```

> **文件存在 ≠ 安装有效**：卸载残留也会留下 `POWERPNT.EXE`。必须查注册表确认：
>
> ```powershell
> Get-ItemProperty 'HKLM:\SOFTWARE\Microsoft\Office\ClickToRun\Configuration' | Select ProductReleaseIds, VersionToReport
> Get-ItemProperty 'HKLM:\SOFTWARE\Microsoft\Office\16.0\PowerPoint\InstallRoot' -Name Path
> ```
>
> `ProductReleaseIds` 有值（如 `ProPlus2024Volume`）+ `InstallRoot\Path` 指向实际目录 ⇒ 有效安装。

### 没有渲染能力时的降级纪律

无法看图时，**不要凭数值硬改视觉**：

- Step 2 / Step 3 只出**诊断清单**（哪页什么问题、建议怎么改），不直接动手；
- 凡"改了会改变视觉节奏"的操作（挪位置、缩尺寸、换排列范式）一律交用户确认；
- **数值指标可以照改**（色值、坐标、比例）——它们可逆且可验证，不依赖肉眼。
